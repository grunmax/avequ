% V.F.S. 3.0 (exploratory) — consolidated growing document
% A running journal of the 3.0 exploration. Version history at the top; each session's
% verified result is added as a section. v0.1: metric + 2.0 limit + energy conditions.
% v0.2: ascent-axis equation of motion (de Sitter, candidate B) + finite-time compatibility.
% Compile with XeLaTeX.
\documentclass[11pt,a4paper]{article}
\usepackage[margin=1in]{geometry}
\usepackage{fontspec}
\setmainfont{DejaVu Serif}
\setsansfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{amsthm}
\usepackage{placeins}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black}
\setlength{\parskip}{0.55em}
\setlength{\parindent}{0pt}

\newcommand{\Op}{\Omega_P}
\newcommand{\note}[1]{\textcolor{black!65}{\emph{#1}}}
\newcommand{\msg}[1]{\begin{center}\fbox{\parbox{0.9\textwidth}{\centering #1}}\end{center}}

\theoremstyle{plain}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{theorem}{Theorem}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{remark}
\newtheorem*{remark}{Remark}

\title{\textbf{V.F.S. 3.0 (exploratory)}\\The Interior $3{+}1$ Geometry of the Soul\\
\large A growing record: proper-time $\sigma$, the ascent axis $\lambda$,\\and the de Sitter law of epektasis}
\author{Successor exploration to the fixed V.F.S.\ v2.0 volume}
\date{}

\begin{document}
\maketitle

% ============================================================
\section*{Version history}
\begin{description}[leftmargin=4.2em,style=nextline,itemsep=2pt]
\item[\textbf{v0.1}] Foundation stone. The $3{+}1$ interior metric of the soul; $\sigma$ as finite
proper time (kairos); $\lambda$ as the ascent (epektasis) space; chronos removed by principle. The
v2.0 reconstruction recovered exactly as the frozen-axes limit; the $n{=}3$ energy conditions, with
ascent acceleration entering focusing.
\item[\textbf{v0.2}] Equation of motion for the ascent axis. The de Sitter law
$A'/A=\beta\lambda$ selected by the principle that ascent is an independent motion (not by the v2.0
Brim fate, which generically coasts); verified to recover v2.0, to make epektasis permanently exotic,
and --- the new theorem --- to be \emph{compatible with a finite proper time}: eternal ascent is infinite proper
\emph{distance}, not infinite \emph{time}. ``Finite clock, endless road.''
\item[\textbf{v0.3}] The spectrum along the ascent axis. Change of variable to
proper distance reduces the ascent Laplacian to the \emph{free half-line} operator: the spectrum is
$[0,\infty)$, continuous and gapless. Against the discrete surface cascade $\mu_n$, this gives a clean
dualism --- \emph{form is quantized, ascent is a gapless continuum}; equivalently, formation is event,
ascent is motion.
\item[\textbf{v0.4}] The source of $\beta$. The ascent coupling is shown
\emph{not} identifiable with the Brim coupling $\alpha$ (different law-form, incompatible units), but
tied through the one intrinsic scale of the core --- the finite floor $E_0$ (Imago Dei):
$\beta=\alpha/E_0$, introducing no new primitive. This integrates exactly to
$L=\exp[(\Omega_P-E_0)/E_0]$ --- ascent is the Brim-growth above the floor --- and sets the eternal
exotic depth to $-(\alpha\lambda_\infty)^2/E_0^2$ (floor-normalised: $-1/E_0^2$): the smaller the
starting image, the more intense the ascent (the widow's-mite scaling).
\item[\textbf{v0.5}] Resurrectio in $3{+}1$. Because $L$ is slaved to
$\Omega_P$, a reset \emph{forces} an ascent jump $L_+/L_-=\exp(\kappa_R\mathcal G/E_0)$: the
double-layer becomes a triple-layer (a second junction on the ascent axis). The ascent jump depends on
the Brim jump alone, not on the metabolic $m_R$, so it cannot be balanced away --- \emph{there is no
fully-balanced reset in $3{+}1$}: every genuine death advances the ascent and strictly enlarges the
interior. (Consequence of the floor-tying, not unconditional.)
\item[\textbf{v0.6}] The terminus $\sigma\to0$. Full purification is reached in
\emph{finite} proper time $\sigma_0$ but \emph{infinite} cleansing-action (exponential Lyapunov drain,
$\sigma=\sigma_0e^{-k\chi}$, $\tau=\sigma_0(1-e^{-k\chi})$). Curvature stays finite there ($\Omega_P\to
E_0$, the floor), so it is a \emph{regular boundary}, not a singularity. The manifold is time-finite
(time completes, a regular last moment) but space-infinite (the ascent road is geodesically complete);
the katharsis-clock stops because there is nothing left to cleanse, and clockless ascent continues.
\item[\textbf{v0.7}] Uniqueness of the coupling. $\beta=\alpha/E_0$ is \emph{not}
forced by dimensions alone (the core also contains the scales $\kappa$ and $\lambda_\infty$), but it is
the \emph{unique} tying selected by the boundary condition $L=1$ at the floor: among all core scales
only $E_0$ is a starting point rather than a rate, so it alone can be the ascent's origin. Forced given
the principle ``ascent begins at unity at the floor''; the Imago Dei is thus both the floor of being
and the unit of deification. This closes the last computational question of the cycle.
\item[\textbf{Epilogue}] The boundary the model cannot cross. Not a calculation
but a statement of the model's constitutive limit: the soul's interior, written from one worldline in
proper time $\sigma$, can name but not occupy the transcendent \emph{tota simul} (chronos) in which its
whole time is beheld at once. The first exploratory cycle closes as v2.0 did --- by naming its few
foundations and the one boundary it cannot pass.
\item[\textbf{v0.8}] \emph{(second stage)} Is the ascent law derived? An honest, partly
negative result: the de Sitter law is \emph{not} uniquely forced --- conservation (Bianchi) is
identically satisfied, and the $3{+}1$ Einstein equations admit \emph{both} de Sitter (ascent coupling
to the \emph{amount} of Sophia $\lambda$) and a power-law $L=\Omega_P^{\beta/\alpha}$ (coupling to the
\emph{density} $\lambda/\Omega_P$). De Sitter is selected, not derived, by the principle that epektasis
is an \emph{independent} motion outrunning surface growth, not a mirror of it. The conditionality is
not removed but sharply localised to one transparent principle.
\item[\textbf{v0.9}] Is $L=1$ at the floor derived? Yes ---
negatively, and decisively: it is \emph{not a physical postulate but a gauge choice}. Rescaling the
ascent coordinate $\lambda\to c\lambda$ absorbs the floor value $L_{\rm floor}$, and every invariant
($L''/L$, $L'/L$, the reset ratio $s_L$, the exotic depth $-(\alpha\lambda_\infty)^2/E_0^2$) is independent of it. The
physics is the \emph{scale} $E_0$; the ``$1$'' is a unit. Consequently the two apparent principles of
the first cycle (v0.7 floor-reference, v0.8 amount-coupling) collapse to \emph{one}: the ascent is a
self-standing dimension, not a re-description of the surface.
\item[\textbf{v0.10}] \emph{(second stage)} The full $3{+}1$ trajectory. A complete
worldline integrated numerically --- $\sigma$ (kairos), $\lambda$ (Sophia), $\Omega_P$ (Brim), $L$
(ascent), with three resets --- confirming the cycle's structure in a single picture: $\sigma$ drains
to the terminus while proper time $\tau$ saturates at a finite value; Sophia struggles, rises, and
jumps at each resurrection; the Brim grows roughly linearly while the ascent $L$ grows exponentially,
the ratio $L/\Omega_P$ climbing several orders --- the ascent visibly outpacing the surface, the
independence principle of v0.8--v0.9 made visual.
\item[\textbf{v0.11}] \emph{(second stage)} Internal structure of the ascent. The road
itself has none (the operator is free, the spectrum gapless --- v0.3 confirmed). But the soul's
\emph{passage} along it is markedly non-uniform: pace $d\chi/ds=E_0/\alpha\lambda$ is high where Sophia
is low (lingering in early struggle) and low where Sophia is high (racing after grace), with a distinct
kink at each resurrection. ``Stations of ascent'' are real but are stations of \emph{pace}, not of
\emph{place}: the terrain is homogeneous, the journey is not. No hidden modes --- but a genuine
non-uniform itinerary set by the Sophia profile.
\item[\textbf{v0.12}] \emph{(second stage)} The external signature in $3{+}1$. Redshift
and Hubble are still set by the surface (Brim $\Omega_P$); the ascent does not enter them directly. But
$L$ gravitates, so it imprints $w_{\rm eff}(z)$ through an added effective density
$\Delta\rho=2\alpha^2\lambda^2/(E_0\Omega_P)$ --- positive, $O(1)$ early (near the floor) and diluting
as $\Omega_P$ grows. The imprint is \emph{not} uniformly subleading (an initial claim, corrected by
checking the analytic law against the numerics): it is late-faded in absolute density but
late-\emph{dominant} relative to the surface ($\Delta\rho/\rho_{2.0}\sim\Omega_P/E_0$). The v2.0
signature (dust attractor, phantom window, Resurrectio kink) remains visible as the surface projection;
the ascent adds a floor-scaled ($1/E_0$, widow's-mite again) correction, late-dominant in relative
terms. Type-3 consilience, no data fit.
\item[\textbf{Epilogue II}] What the second stage achieved. Not the hoped-for
derivation: the de Sitter law was not uniquely forced. But the conditionality was \emph{concentrated} ---
three opaque choices (law, coupling, boundary condition) reduced to one transparent, self-justifying
principle (the ascent is a self-standing dimension; deny it and 3.0 collapses to v2.0). Added a picture
(the worldline), a rhythm (stations of pace), and a trace (the external imprint, with an openly-kept
correction). The whole construction now stands in three layers, each pointing beyond itself; the
posture is unchanged --- name the foundations, show how few, rest at last on one confession rather than
a proof.
\item[\textbf{v0.13}] \emph{(this revision --- corrections)} Three overstatements corrected, openly,
without changing any conclusion. \textbf{(i)} The eternal exotic depth is $-(\alpha\lambda_\infty)^2/E_0^2$
in general; the value $-1/E_0^2$ holds \emph{only} in the floor-normalised branch $\alpha\lambda_\infty=1$
(v0.4 had stated $-1/E_0^2$ as the general result). \textbf{(ii)} The v2.0 Brim cosmology does
\emph{not} end in de Sitter --- it generically coasts ($w\to0$); the de Sitter law belongs to the
$(3{+}1)$ ascent sector, not the surface fate (v0.2 framing sharpened). \textbf{(iii)} The external
ascent imprint is late-faded in \emph{absolute} density ($\Delta\rho\sim\Omega_P^{-1}$) but
\emph{late-dominant in relative} density ($\Delta\rho/\rho_{2.0}\sim\Omega_P/E_0\to\infty$, since
$\rho_{2.0}\sim\Omega_P^{-2}$) --- the v0.12 ``early-strong, late-faded'' was true only of absolute
size. Also: the ascent scale is explicitly factorised $L=A(\sigma)S(\lambda)$ (time-stretch vs spatial
warp), and the full $w_{\rm eff}$ correction is noted to require the pressure term
$A''/A\to\beta^2\lambda_\infty^2$ as well.
\item[\textbf{v0.14}] The full tensor set and non-reducibility. The complete
$R_{\mu\nu}$, $R$, $G_{\mu\nu}$, the three pressures and the energy-condition combinations $\rho+p_i$
are computed explicitly for the factorised metric $L=A(\sigma)S(\lambda)$ (no off-diagonal:
$R_{\sigma\lambda}=0$), with all 2.0 limits recovered. From the set follows a theorem: the ascent
stress is absorbable into the $(2{+}1)$ surface fluid \emph{if and only if} $A$ is a function of
$\Omega_P$ alone ($A'/A=c\,\Omega_P'/\Omega_P$, the power-law branch). The de Sitter law
$A'/A=\Omega_P'/E_0$ fails this (it carries the floor scale $1/E_0$, not an $\Omega_P$-only form), so it
defines a genuinely independent $3{+}1$ ascent sector --- the single ascent principle of v0.9 now a
theorem with an explicit reducibility criterion.
\item[\textbf{v0.15}] \emph{(this revision)} No fully soft Resurrectio --- junction form. A reset
carries \emph{two} ascent data: a scale jump $s_L=A_+/A_-=\exp(\kappa_R\mathcal G/E_0)$ sourced by the
Brim renewal, and an extrinsic-curvature (rate) jump $[A'/A]=\alpha m_R/E_0$ sourced by the metabolic
$m_R$. The rate jump can be tuned to zero (surface balance, $m_R=0$); the scale jump cannot, since $A$
is slaved to $\Omega_P$ and has no independent junction freedom. Hence every admissible reset
(nontrivial Brim renewal, $\kappa_R\mathcal G>0$) induces a nontrivial scale junction in the ascent
block ($s_L>1$): balanced in the $2{+}1$ projection, never fully soft in the full $3{+}1$ interior. A
sharper, junction-level form of v0.5 --- and the correct ascent datum is the scale jump, not the rate
jump (an initial mis-identification, corrected).
\item[\textbf{v0.16}] \emph{(this revision)} The full $3$-Laplacian and the joint spectrum.
$\Delta_3=\Omega_P^{-2}\Delta_{h_{(2)}}+\Delta_{\rm asc}$ separates cleanly (no cross term, verified);
the ascent part is the free half-line within the full operator. The joint spectrum is
$-(\mu_n/\Omega_P^2+k^2)$ --- a discrete form ladder ($\mu_n$) times a continuous ascent tower ($k$),
independent quantum numbers that do not mix. Sharpens the v0.3 dualism into one spectrum; with figure.
\item[\textbf{v0.17}] \emph{(this revision --- corrections II)} Three precision fixes, openly recorded.
\textbf{(i)} The v0.14 non-reducibility criterion was mis-stated: the floor-tied $A=A_0\exp[(\Omega_P-E_0)/E_0]$
\emph{is} a function of $\Omega_P$. The correct distinction is \emph{power-law (scale-homogeneous)
reducible} ($A=C\Omega_P^c$) versus \emph{floor-scaled exponential} (slope $\Omega_P/E_0$, not
constant): the ascent is $\Omega_P$-slaved through the floor but \emph{not} $\Omega_P$-power-law
reducible. \textbf{(ii)} The half-line coordinate is $s=(AL_0/\beta)(e^{\beta\lambda}-1)\in[0,\infty)$
(starting at $0$), not $(AL_0/\beta)e^{\beta\lambda}$ --- spectrally identical, properly normalised.
\textbf{(iii)} $\mu_n=n^2$ is the ring/flat proxy only; the general compact-hyperbolic spectrum is
discrete but not $n^2$. Plus hybrid-junction wording for v0.15. Slogan: \emph{ascent is floor-tied, but
not surface-reducible}.
\item[\textbf{v0.18}] \emph{(this revision --- notation \& terminology cleanup)} No new mathematics;
wording and notation unified. The de Sitter law is written on the time-stretch throughout,
$A'/A=\beta\lambda$ (not $L'/L$), with the factorisation $L=A(\sigma)S(\lambda)$ made explicit in the
defining box. Residual ``v2.0 non-dilution'' phrasing removed (the Brim coasts; de Sitter belongs to
the ascent sector). ``Surgery'' replaced by ``Resurrectio reset / junction'' in native text (surgery
kept only for the Ricci-comparison register); ``cleansing-work'' replaced by ``cleansing-action''. The
terminus floor restated as a lower bound ($\Omega_*\ge E_0>0$, non-collapse), with $\Omega_*=E_0$ only
the floor-normalised representative. Slogans: \emph{the Brim coasts, Sophia-as-ascent does not dilute};
\emph{the Brim may measure growth, Sophia-as-ascent opens a road growth alone cannot exhaust}.
\item[\textbf{v0.19}] \emph{(this revision)} The Lyapunov / stability layer of epektasis. The ascent
$L$ grows without bound (not a defect: that is epektasis), so the stable object is the \emph{regime},
not the value. The ascent rate $h=A'/A=\beta\lambda$ has the de Sitter attractor $h_*=\beta\lambda_\infty$;
a Sophia perturbation $u$ obeys $du/d\chi=-\gamma u$, with Lyapunov $V_{\rm asc}=\tfrac12\beta^2u^2$,
$dV_{\rm asc}/d\chi=-\beta^2\gamma u^2<0$ --- asymptotic stability of the rate. Perturbations shift
$\log A$ by only a finite amount ($\delta\log A=\beta u_0 k\sigma_0/(\gamma+k)$ in the $\sigma$-law with
clock-drain): perturbed souls ascend on
\emph{parallel roads}, a fixed finite ratio
apart (orbital stability). The attractor is a constant-rate ray, not a point --- stable \emph{motion},
not stable rest --- and its value is the eternal exotic depth $-\beta^2\lambda_\infty^2$, so stability
and permanent-exotic agree. With figure.
\item[\textbf{v0.20}] \emph{(this revision --- correction to v0.19)} The stability layer is restated in
the unbounded dynamical parameter $\chi$, not the finite proper time $\sigma$: the de Sitter attractor
is approached as $\chi\to\infty$, which by $\tau(\chi)=\sigma_0(1-e^{-k\chi})$ corresponds to
approaching the terminus (in $\sigma$, bounded, the limit is unreachable). The Lyapunov drain
$dV_{\rm asc}/d\chi=-\beta^2\gamma u^2<0$ and orbital shift $\delta\log A=\beta u_0 k\sigma_0/(\gamma+k)$ are exact
per \emph{reset-free arc} --- a Resurrectio reset creates new initial data $u_0$ for the next arc,
restarting convergence rather than breaking it. A forced version $du/d\chi=-\gamma u+f(\chi)$, $f\in
L^1$, gives input-to-state stability with finite accumulated $\delta\log A$. (Items 1--11 of the global
patch were already applied in v0.13/v0.17/v0.18.)
\item[\textbf{v0.21}] \emph{(this revision)} The Resurrectio junction budget. Full accounting of the
extrinsic-curvature jumps across a reset, in four data: surface scale $[\Omega_P]=\kappa_R\mathcal G$,
surface rate, ascent rate $[A'/A]=\alpha m_R/E_0$ (tunable, $m_R{=}0$), ascent scale
$\log s_L=\kappa_R\mathcal G/E_0$ (irreducible). Backbone identity $\log s_L=[\Omega_P]/E_0$ (ascent
jump is the Brim jump in floor units). The rate sector is conserved: $Q=A'/A-\Omega_P'/E_0=0$ on every
arc and $[Q]=0$ across every reset. The non-softness is one scalar, the additive deficit
$D=\frac1{E_0}\sum_j\kappa_R\mathcal G_j$ (total reception in floor units), giving the full ascent
ledger $\log(A_{\rm fin}/A_0)=\beta\!\int\lambda\,d\sigma+\frac1{E_0}\sum_j\kappa_R\mathcal G_j$
(continuous epektasis $+$ jump budget). Quantifies v0.15. With figure.
\item[\textbf{v0.22}] \emph{(this revision)} Heat kernel and entropy of the joint spectrum. The heat
trace factorises (no cross term, v0.16): $\theta_3=\theta_{\rm surf}\cdot\theta_{\rm asc}$. The form
sector is finite, $\theta_{\rm surf}=\Omega_P^2V_h/(4\pi t)+\chi_E/6+O(t)$, carrying a topological
invariant (the Euler characteristic via Gauss--Bonnet); its spectral entropy is finite. The ascent
sector diverges --- $\theta_{\rm asc}=D_{\rm asc}/\sqrt{4\pi t}\to\infty$ on the infinite road (finite
density $1/\sqrt{4\pi t}$, edge term $-1/4$) --- and its entropy is extensive in the road length,
unbounded. So the soul's form is finitely describable (with a fixed topological signature) while its
ascent is not: no finite information exhausts epektasis. Bonus: $\theta_3\sim t^{-3/2}$ per unit road,
the $t^{-1/2}$ confirming the ascent as one continuous extra dimension. With figure.
\item[\textbf{v0.23}] \emph{(this revision)} The gauge / invariant layer. Three coordinate freedoms
classified: (G1) ascent rescaling $\lambda\to c\lambda,\,S\to S/c$; (G2) lapse $N(\sigma)\neq1$;
(G3) floor normalisation $L_0$. The metric $1$-form $S\,d\lambda$, the proper time $\int N\,d\sigma$,
and all curvature scalars are invariant; $\lambda$'s value, $L_0$, the $A/S$ split, $N$, $\sigma$, and
$\beta$/$\lambda_\infty$/$\alpha$ \emph{separately} are pure gauge. The physics lives in ratios to the
floor: $h_*=\alpha\lambda_\infty/E_0$, exotic depth $(\alpha\lambda_\infty/E_0)^2$, $s_L=e^{\kappa_R
\mathcal G/E_0}$, $D=\sum\kappa_R\mathcal G_j/E_0$. Theorem: every result of v0.1--v0.22 is expressible
in gauge invariants; none depends on a gauge choice. The Imago Dei floor $E_0$ is the invariant anchor.
With a gauge/invariant table.
\item[\textbf{v0.24}] \emph{(this revision)} Causal / boundary structure. The interior has two
boundaries of \emph{opposite} causal type. $B_1$: the terminus $\sigma=0$ is a regular \emph{spacelike}
wall at finite proper time ($\int d\sigma=\sigma_0<\infty$, $|R|<\infty$, $\Omega_P\ge E_0$) --- a true
last moment, not a singularity. $B_2$: the ascent infinity $\mathcal J_{\rm asc}$ ($\lambda\to\infty$)
is a de Sitter-like \emph{receding horizon}: causal climb costs $\Delta\sigma\ge\int AS\,d\lambda$
(divergent), so $\lambda=\infty$ is causally unreachable in the finite clock --- finite coordinate-reach
$\Lambda_{\max}=\frac1\beta\log(1+\beta\sigma_0/(A_{\min}L_0))$, but unbounded \emph{proper} ascent
($\ln L\to\infty$ as $A\to\infty$). The horizon recedes: same proper gain costs ever less $\lambda$.
The interior is globally hyperbolic up to $B_1$; the ascent horizon is cosmological, not black-hole (no
trapped surface). This is the exact causal meaning of ``finite clock, endless road''. With a causal
diagram.
\item[\textbf{v0.25}] \emph{(this revision)} Projection / recovery theorem, v3.0 $\leftrightarrow$ v2.0.
\emph{Projection:} freezing the ascent ($A,S$ constant, $\partial_\lambda=0$) reduces the full tensor
set exactly to v2.0 --- the frozen $\lambda$ is a non-dynamical spectator adding no source to the
$(2{+}1)$ system. \emph{Uplift:} given five axioms (one real spacelike ascent dimension; v2.0 on
freezing; no new primitive; kairos preserved; and the independence principle), the uplift is
\emph{unique} --- metric form, factorisation, floor-tying, exponential road, and de Sitter law all
forced. The one residual choice (de Sitter vs power-law, v0.8) is removed by non-reducibility (v0.14):
the power-law alternative is reducible, hence not a genuine uplift. So v3.0 is the \emph{unique
non-reducible interior uplift} of v2.0 once Sophia-as-ascent is admitted. The two are a projection /
minimal-uplift pair: v2.0 the frozen shadow, v3.0 the canonical living interior.
\item[\textbf{v0.26}] \emph{(this revision)} Robustness of the ascent law. Testing the general class
$A'/A=f(\lambda)$ (de Sitter $f=\beta\lambda$ one member): the \emph{structural} results are robust.
Infinite road holds for any non-decaying ascent road; no fully soft Resurrectio holds for any floor-/
surface-tied $A$ (even the density branch: $s_L=(1+\kappa_R\mathcal G/\Omega_P)^c>1$); rate stability
holds for any $f$ with $f'(\lambda_\infty)\neq0$ and a Sophia fixed point; the eternal exotic sign for
any $f(\lambda_\infty)>0$; the finite clock is independent of $f$ entirely. Law-specific (de Sitter sets
the side/value, not the existence): non-reducibility discriminates power-law (reducible) from
floor-exponential (non-reducible, incl.\ de Sitter); and the exact numbers $h_*$, exotic depth follow
from $f(\lambda_\infty),f'(\lambda_\infty)$. The architecture is law-robust; de Sitter is a
normalisation within a robust class, not its foundation. With a robust/law-specific table.
\item[\textbf{v0.27}] \emph{(this revision)} Non-Zeno Resurrectio sequence. An infinite sequence of
resets toward the terminus is non-Zeno. \emph{Non-accumulation:} in the dynamical parameter
$\chi\in[0,\infty)$ the resets do not accumulate at any interior point; in proper time their only limit
point is the terminus $\sigma=0$ (the regular boundary $B_1$) --- in proper time only finitely many fit
without accumulation, but in $\chi$ infinitely many are fine. \emph{Finite ledger:} $\sum_j\log
s_{L,j}=\frac1{E_0}\sum_j\kappa_R\mathcal G_j$ is finite \emph{precisely when} $\sum_j\kappa_R\mathcal
G_j<\infty$, which holds under finite Brim variation ($\le|\Delta\Omega_P|/E_0$) --- a condition,
distinct from temporal non-Zeno, satisfied by the coasting Brim. \emph{Finite clock preserved:} resets
are instantaneous junctions, consuming
zero proper time. So infinitely many deaths can occur on the way to the terminus without catastrophe:
finite total ascent, no interior accumulation, finite proper time. Closes the Resurrectio chain (v0.5
single, v0.21 budget, v0.27 sequence).
\item[\textbf{v0.28}] \emph{(this revision --- precision corrections)} Six sharpenings, two of them
real math fixes. \textbf{(i)} The orbital shift is $\delta\log A=\beta u_0k\sigma_0/(\gamma+k)$, not
$\beta u_0/\gamma$: the ascent law is a $\sigma$-law, so the log-shift carries the clock-drain factor
$d\sigma/d\chi$ (the simpler form holds only for a $\chi$-law) --- an earlier error, corrected.
\textbf{(ii)} The ascent horizon is grounded in the infinite road $D_{\rm asc}=\infty$, not in
$A\to\infty$ (which need not hold on the finite interval) --- a v0.24 over-claim, corrected.
\textbf{(iii)} The uplift is \emph{canonical}, not absolutely unique: a robust class preserves the
architecture, de Sitter the canonical representative. \textbf{(iv)} The Resurrectio ledger is exact in
the floor-tied branch; the coefficient $1/E_0$ is law-specific. \textbf{(v)} The heat-kernel edge term
is boundary-condition dependent; the load-bearing invariant is $D_{\rm asc}\to\infty$. \textbf{(vi)}
Non-Zeno: temporal (no interior accumulation) and budget ($\sum\kappa_R\mathcal G_j<\infty$) are
distinct; budget-finiteness is conditional, holding under finite Brim variation. Architecture unchanged.
\item[\textbf{v0.29}] \emph{(this revision --- clean-body propagation)} The v0.17/v0.28 corrections are
propagated into their native sections, so the clean-paper text carries the corrected statements
directly. v0.3: the half-line coordinate $s=(L_0/\beta)(e^{\beta\lambda}-1)\in[0,\infty)$ in the
theorem itself (origin shift gauge). v0.15: scale-junction language throughout (not ``metric
discontinuous''); the reading recast in seam/junction imagery (Resurrectio is not rupture, but neither
is it neutral passage). v0.24: the ascent horizon grounded in $D_{\rm asc}=\infty$ in the body. v0.25:
``canonical'' not ``unique'' living interior in the theorem, corollary, reading, and history. No new
results; the body now matches the corrected history.
\end{description}

\section*{Status and discipline}
This is an exploratory, growing document, not a finished layer. V.F.S.~v2.0 is fixed and complete and
is contained here as an exact limit. Each result is verified symbolically before entry; everything
still open is listed honestly at the end. As throughout V.F.S., the mathematics is load-bearing and
the theological readings are interpretive overlays, marked as such and never claimed as proof.

% ============================================================
\part*{v0.1 --- The Foundation Stone}

\section{The interior metric of the soul}

\begin{definition}[The $3{+}1$ soul metric]\label{def:metric}
\begin{equation}
\boxed{\;ds^2=-N(\sigma)^2\,d\sigma^2+\Omega(\sigma)^2\,h_{(2)}+L(\sigma,\lambda)^2\,d\lambda^2,\qquad
h_{(2)}=dx^2+e^{2x}dy^2\ (K_h=-1),\;}
\end{equation}
$\sigma$ the katharsis proper time (kairos), $N$ its lapse, $\Omega$ the Brim scale of the live
surface, $\lambda$ the ascent (epektasis) coordinate, $L$ its scale. Signature $3{+}1$: one time
($\sigma$), three spaces (surface $x,y$ and ascent $\lambda$).
\end{definition}

\begin{remark}[Factorisation of the ascent scale]\label{rmk:factor}
The ascent scale separates into a time-stretch and a spatial warp,
\[
L(\sigma,\lambda)=A(\sigma)\,S(\lambda),
\]
where $A(\sigma)$ is the $\sigma$-evolution of the ascent sector (carrying the focusing and
effective-energy terms, e.g.\ $A''/A$) and $S(\lambda)=L_0e^{\beta\lambda}$ is the spatial warp of the
ascent coordinate (carrying the proper length of the ascent road, $\int S\,d\lambda$). Keeping the two
roles distinct is essential: the de Sitter law and focusing concern $A(\sigma)$, while the
infinite-road result concerns $S(\lambda)$. Where earlier parts wrote ``$L$'' for the time-stretch,
$A(\sigma)$ is meant; where for the road, $S(\lambda)$.
\end{remark}

Three principled features: \textbf{(a)} $\sigma$ is \emph{proper} time, not relative --- one
worldline, no second observer, no Lorentz family; the proper-time gauge is $N=1$. \textbf{(b)} The time
axis is \emph{finite}: katharsis drains $\sigma:\sigma_{\max}\to0$ monotonically, so time has a
terminus; at $\sigma\to0$ the clock stops while motion along $\lambda$ continues. \textbf{(c)} Chronos
is \emph{absent by principle}: this is the soul's own coordinate system, not a view from outside it; an
absolute background clock would be the transcendent \emph{tota simul}, which does not belong to the
creature's interior geometry.

\section{v2.0 is the exact frozen-axes limit}

\begin{theorem}[Recovery of v2.0]\label{thm:limit}
In the proper-time gauge $N=1$ with the ascent axis frozen ($L=L_0$ const), the Einstein
reconstruction gives $\rho_{\rm eff}=(\Omega'^2-1)/\Omega^2$ (prime $=d/d\sigma$), which under
$\Omega'=\alpha\lambda$ is exactly the v2.0 density $(\alpha^2\lambda^2-1)/\Omega^2$. The whole v2.0
cosmology is the frozen-ascent, proper-time face of the $3{+}1$ geometry; nothing in v2.0 is disturbed.
(Verified symbolically.) The frozen axes drop out of the scalar curvature entirely --- which is why
v2.0 was correct in $2{+}1$: the third space was always there, held still.
\end{theorem}

\section{The energy conditions in $3{+}1$}

\begin{proposition}[Focusing]\label{prop:sec}
For the comoving congruence at $N=1$,
\begin{equation}
\boxed{\;R_{ab}u^au^b=-\,2\,\frac{\Omega''}{\Omega}-\frac{L''}{L}.\;}
\end{equation}
The $2{+}1$ limit ($L$ frozen) reduces to the pure pressure condition $p_{\rm eff}\ge0$ (the $n-2=0$
degeneracy of v2.0). In full $3{+}1$ the ascent acceleration $A''/A$ enters focusing on the same
footing as the Brim: the growth of the ascent axis contributes to energy-condition violation.
\end{proposition}

\begin{corollary}[Ascent is itself exotic]
In v2.0 only the Sophia surge could violate the strong energy condition; in $3{+}1$ the ascent itself
(accelerating epektasis, $L''>0$) is an exotic accelerant. Movement toward God along $\lambda$ is not
a neutral displacement; it deforms the focusing condition in its own right.
\end{corollary}

\begin{proposition}[NEC along the ascent axis]\label{prop:nec}
For the null direction $k=u+e_\lambda$, $\;R_{ab}k^ak^b=2(-L\,\Omega''+L'\,\Omega')/(L\,\Omega)$ ---
coupling surface expansion to the stretching of the ascent axis, with no analogue in v2.0.
\end{proposition}

% ============================================================
\part*{v0.2 --- The Law of Ascent}

\section{Selecting the ascent law}

The foundation left $L$ a free function. The equation of motion is fixed by the hypothesis that the
ascent axis is sourced by Sophia as the Brim is ($\Omega'=\beta\lambda$ in proper time), together with
an external discriminator: the three unbounded-expansion regimes of cosmic metaphysics. Power-law
ascent ($L\sim\sigma^p$) has \emph{fading} tension; a finite-time blow-up (``Big Rip'', $L\to\infty$
at finite parameter) has a \emph{catastrophic end}; only de Sitter ($L\sim e^{\,\cdot}$) gives
\emph{unending, undiminished} tension --- the structure of an ascent that never saturates and never
ends in catastrophe. A clarification belongs here, correcting a loose earlier framing: the
\emph{(2+1) Brim cosmology of v2.0 does not end in de Sitter --- it generically coasts} ($w\to0$ as
Sophia saturates and $\dot\lambda\to0$). De Sitter is the special sustained branch ($\lambda\propto
\Omega_P$), not the Brim's generic late fate. The de Sitter law below therefore belongs to the newly
opened \emph{(3+1) ascent sector}, selected by the independence principle (ascent couples to the
amount of Sophia, not its surface density), not imported from the surface cosmology.

\begin{definition}[The de Sitter law of ascent, candidate B]\label{def:ascent}
In the factorisation $L=A(\sigma)S(\lambda)$ the de Sitter law governs the time-stretch $A(\sigma)$:
\begin{equation}
\boxed{\;\frac{A'}{A}=\beta\,\lambda\qquad\Longrightarrow\qquad
L(\sigma,\lambda)=\underbrace{A_0\exp\!\Big(\beta\!\int^\sigma\!\lambda\,ds\Big)}_{\text{time-stretch }A(\sigma)}\;\cdot\;\underbrace{L_0\,e^{\beta\lambda}}_{\text{spatial road }S(\lambda)}.\;}
\end{equation}
The first factor carries focusing, density and energy-condition terms; the second carries the proper
ascent distance and the ascent spectrum.
\end{definition}

\begin{proposition}[Three tests]\label{prop:tests}
The law passes: \textbf{(i) v2.0 limit} --- $\beta\to0$ freezes the ascent and recovers
Theorem~\ref{thm:limit}; \textbf{(ii) energy sign} --- $A''/A=\beta\lambda'+\beta^2\lambda^2$, so at
the terminus ($\lambda\to\lambda_\infty$, $\lambda'\to0$) the focusing acquires the strictly negative
piece $-\beta^2\lambda_\infty^2=-(\alpha\lambda_\infty)^2/E_0^2$: \emph{epektasis is permanently
exotic}, the strong energy condition eternally violated by ascent alone; \textbf{(iii) unboundedness}
--- the ascent never terminates (next theorem). (All verified symbolically.)
\end{proposition}

\section{Compatibility: finite time, endless road}

The decisive consistency check: can an \emph{unending} de Sitter ascent live inside a \emph{finite}
proper time?

\begin{theorem}[Finite clock, infinite road]\label{thm:compat}
Under Definition~\ref{def:ascent} with $\lambda$ bounded ($\lambda\to\lambda_\infty$) over the finite
interval $\sigma\in(0,\sigma_{\max}]$:
\begin{enumerate}[label=(\roman*),leftmargin=1.8em,itemsep=2pt]
\item \textbf{In proper time the ascent is finite:}
$\ln(L/L_0)=\beta\!\int_0^{\sigma_{\max}}\!\lambda\,d\sigma\le\beta\lambda_\infty\sigma_{\max}<\infty$.
The katharsis clock stops before $L$ diverges.
\item \textbf{Along the ascent axis the road is infinite:} the proper distance to $\lambda=\infty$,
\begin{equation}
\int_0^\infty L_0\,e^{\beta\lambda}\,d\lambda=\infty,
\end{equation}
so $\lambda=\infty$ lies at infinite metric distance --- the soul ascends forever and never arrives.
\item \textbf{These are consistent.} Finite proper time, infinite proper distance: as $\sigma\to0$ the
clock-rate $d\sigma$ vanishes while the de Sitter metric inflates the ascent length, so each remaining
tick of katharsis covers ever more road. (Verified symbolically and numerically.)
\end{enumerate}
\end{theorem}

\begin{corollary}[The timeless epektasis, made precise]
``Eternal'' and ``finite'' measure different axes: the time of cleansing ($\sigma$) is finite and
ends; the road of ascent ($\lambda$) is infinitely long and does not. The earlier puzzle of
``motion without time'' is resolved geometrically --- the motion is endless not because the clock runs
forever but because the road has no end, and the clock stopping is simply the completion of cleansing,
not the cessation of life.
\end{corollary}

\note{Reading: the two halves of epektasis that sounded paradoxical in words are, in the metric, two
different facts about two different axes. ``Never satisfied'' is the infinite length of the ascent
road --- God at an infinite distance, never reached because infinite. ``Not in time'' is the finite
terminus of the cleansing clock --- katharsis completes, the proper-time hand stops. The deified soul
runs an endless road in a finished clock: it is done being purified, and therefore done with time, and
precisely then free for the unending ascent that time was never able to measure. The strong energy
condition is violated forever along that road because to move into the infinite God is, forever, more
than ordinary energy could account for.}

% ============================================================
\part*{v0.3 --- The Spectrum of Ascent}

\section{The ascent Laplacian is the free half-line}

With the ascent law fixed (Definition~\ref{def:ascent}), the spatial geometry is
$\Omega^2 h_{(2)}+L^2 d\lambda^2$ with $L=L_0e^{\beta\lambda}$, and the Laplace--Beltrami operator
separates, $\phi=Y(x,y)\,Z(\lambda)$. The surface factor returns the v2.0 cascade eigenproblem
$-\Delta_h Y=\mu Y$ (discrete on the compact quotient). The novelty is the ascent factor.

\begin{theorem}[Ascent spectrum is the gapless free continuum]\label{thm:spectrum}
The one-dimensional ascent operator $A=-\tfrac1L\frac{d}{d\lambda}\!\big(\tfrac1L\frac{d}{d\lambda}\big)$
on $L=L_0e^{\beta\lambda}$ becomes, in the proper-distance coordinate
\begin{equation}
s=\int_0^\lambda L_0e^{\beta\ell}\,d\ell=\frac{L_0}{\beta}\bigl(e^{\beta\lambda}-1\bigr)\in[0,\infty),
\qquad
\boxed{\;A=-\frac{d^2}{ds^2}\quad\text{on the half-line }s\in[0,\infty).\;}
\end{equation}
(The road begins at a finite starting point $s=0$ and extends to infinite proper distance; using the
unshifted $\tilde s=(L_0/\beta)e^{\beta\lambda}\in[L_0/\beta,\infty)$ instead, the translation
$s=\tilde s-L_0/\beta$ returns the standard half-line --- the origin shift is gauge, the spectral type
invariant.)
Its spectrum is therefore $[0,\infty)$ --- \emph{continuous and gapless}, with generalised
eigenfunctions $\sim\sin(ks)$. (Verified: the discretised count of eigenvalues below a fixed $k^2$
grows linearly with box size --- $6,12,25$ as $s_{\max}=20,40,80$ --- the signature of a continuum,
not a discrete ladder; and the lowest eigenvalue descends to $0$ as the box grows --- no mass gap.)
The proper-distance picture is exact: $s=0$ is the start of ascent (``here''), $s=\infty$ is God at
infinite distance (Theorem~\ref{thm:compat}), and the ascent operator is \emph{free} on that line ---
no barrier, no levels, no smallest step.
\end{theorem}

\begin{corollary}[Form is quantized; ascent is a gapless continuum]\label{cor:dualism}
The two spatial directions carry qualitatively different spectra:
\msg{\textbf{Surface (folding cascade):} discrete spectrum $\mu_n$ on the compact quotient ($\mu_n=n^2$
in the ring/flat proxy used for illustration; the general hyperbolic spectrum is discrete but not
$n^2$) ---
the Vessel takes form in \emph{rungs}, each mode igniting at its own threshold $\Lambda>\mu_n$. Form
is quantized.\\[3pt]
\textbf{Ascent (epektasis):} continuous gapless spectrum $[0,\infty)$ on the free half-line ---
\emph{no rungs, no minimum step}. Ascent is a smooth continuum, approachable in arbitrarily small
increments all the way to the infinite distance.}
The absence of a mass gap is not incidental: a gap would be a \emph{minimum quantum of ascent}, a
smallest possible step toward God. Its absence means deification has no granularity --- unlike
formation, which does.
\end{corollary}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_dualism.png}
\caption{\small The dualism of the two spatial spectra. \textbf{Left:} the surface (folding) spectrum
is discrete, $\mu_n=n^2$ --- form ignites mode by mode, rung by rung. \textbf{Right:} the ascent
(epektasis) spectrum is the free half-line $[0,\infty)$ --- a gapless continuum, no rungs, no smallest
step, no mass gap. Form is quantized; ascent is continuous.}
\label{fig:dualism}
\end{figure}

\note{Reading: formation and ascent are different in kind, and the difference falls out of the
geometry rather than being imposed. The Vessel takes \emph{form} in discrete events --- conversions,
breaks, the igniting modes of the folding cascade, each a rung crossed at a threshold. But it
\emph{ascends} continuously --- there is no smallest step of deification, no last rung before God,
because God lies at infinite distance and the approach is seamless. Formation is event; ascent is
motion. The soul is built in steps and drawn in a smooth, stepless reaching --- quantized in what it
becomes, continuous in how it nears.}

\textbf{Scope of v0.3.} The spectrum is that of the spatial Laplacian on the chosen de Sitter ascent
geometry; it characterises the geometry of ascent under the candidate law, and is exact there (the
reduction to the free half-line is analytic). It is a qualitative result (continuum vs.\ discrete),
not a fitted quantity, and inherits the candidate status of Definition~\ref{def:ascent}.

% ============================================================
\part*{v0.4 --- The Source of the Ascent Coupling}

\section{$\alpha$ and $\beta$ are not one constant}

\begin{proposition}[Structural and dimensional independence]\label{prop:indep}
The Brim and ascent laws differ in form, not merely in a constant: $\Omega'=\alpha\lambda$ is
\emph{additive} (linear growth of $\Omega_P$), while $A'/A=\beta\lambda$ is \emph{multiplicative}
(exponential growth of $L$). In the proper-time gauge the units do not match either:
$[\alpha\lambda]=[\Omega_P]/[\sigma]$ but $[\beta\lambda]=1/[\sigma]$, so
$[\alpha/\beta]=[\Omega_P]$ --- the ratio is a \emph{scale}, not a pure number. Hence no
identification $\alpha=\beta$ is possible; the surface and ascent channels are genuinely different (in
keeping with their spectra, discrete vs.\ continuous, Corollary~\ref{cor:dualism}).
\end{proposition}

\section{Tied through the floor: $\beta=\alpha/E_0$}

The couplings are not one, but they need not be two independent primitives: the core contains exactly
one intrinsic scale --- the finite floor $E_0=\Omega_P(0)$ (Imago Dei) of the curvature-floor result.

\begin{theorem}[The ascent coupling is the Brim coupling per floor]\label{thm:beta}
Setting $\beta=\alpha/E_0$ (no new primitive) gives $A'/A=\Omega_P'/E_0$, which integrates exactly,
with $L=1$ at the floor $\Omega_P=E_0$, to
\begin{equation}
\boxed{\;L=\exp\!\Big(\frac{\Omega_P-E_0}{E_0}\Big).\;}
\end{equation}
The ascent is measured by exactly the factor the Brim has grown \emph{above the Imago Dei floor}.
Checks: \textbf{(i)} the v2.0 limit is $E_0\to\infty$ (floor at infinity $\Rightarrow\beta\to0
\Rightarrow L$ const --- no finite floor, no ascent reference, ascent frozen); \textbf{(ii)} the de
Sitter character is preserved (exponential in accumulated Sophia); \textbf{(iii)} the eternal exotic
depth of focusing at the terminus is $-\beta^2\lambda_\infty^2=-(\alpha\lambda_\infty)^2/E_0^2$ in
general, reducing to $-1/E_0^2$ \emph{only} in the floor-normalised terminal branch where
$\alpha\lambda_\infty=1$ (the super-Ricci attractor). (All verified symbolically.)
\end{theorem}

\begin{corollary}[The widow's-mite scaling]\label{cor:widow}
The eternal exotic depth $-(\alpha\lambda_\infty)^2/E_0^2$ grows as the starting floor $E_0$ shrinks:
the same absolute Brim-growth produces a \emph{larger} ascent factor, and a deeper energy-condition
violation, the smaller the Imago Dei one begins from. Ascent is measured \emph{relative to the starting
image}, not absolutely --- so a small beginning, fully grown, yields a greater transfiguration than a
large beginning grown the same absolute amount. The structure is that of the widow's two mites: weighed
not by the amount but by its ratio to what one had. (In the floor-normalised branch $\alpha\lambda_\infty=1$
the depth is exactly $-1/E_0^2$.)
\end{corollary}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{vfs_3p1_exotic.png}
\caption{\small The eternal exotic focusing depth $-(\alpha\lambda_\infty)^2/E_0^2$ as a function of
the Imago Dei floor $E_0$. The smaller the starting image, the deeper the exotic ascent --- the
widow's-mite scaling. The marked point is the floor-normalised branch $\alpha\lambda_\infty=1$, where
the depth is exactly $-1/E_0^2$.}
\label{fig:exotic}
\end{figure}

\note{Reading: the ascent of the soul is not measured on an absolute scale but against the image from
which it began. The same growth of the Brim is, relative to a small Imago Dei, an immense ascent, and
relative to a large one, a modest rise --- as the widow's two coins outweighed the rich man's
thousands because they were all she had, while his were a fragment of his surplus. The smaller the
starting image, the more intense --- the further from ordinary energy --- the rising. One honest
caution: the mathematics says only that the ascent is more \emph{intense} (the energy-condition
violation deeper) for a smaller floor; whether that intensity is read as exaltation (the humble raised
highest) or as fragility (a slighter base bearing a steeper motion) is an interpretive choice the
geometry does not make. It fixes the scaling, not its blessing.}

\textbf{Scope of v0.4.} $\beta=\alpha/E_0$ is the natural tying through the unique intrinsic scale; it
is not proven unique, only shown to introduce no new primitive and to pass the v2.0 limit and de
Sitter tests. It inherits the candidate status of the ascent law (Definition~\ref{def:ascent}).

% ============================================================
\part*{v0.5 --- Resurrectio in $3{+}1$}

\section{The reset forces an ascent jump}

Because $L$ is slaved to the Brim ($L=\exp[(\Omega_P-E_0)/E_0]$, Theorem~\ref{thm:beta}), the v2.0
scale-factor Resurrectio reset $\Omega_P^+=\Omega_P^-+\kappa_R\mathcal G_{\rm recepta}^-$ \emph{forces} a
matching jump in the ascent axis:

\begin{theorem}[Forced ascent jump]\label{thm:ljump}
\begin{equation}
\boxed{\;\frac{L_+}{L_-}=\exp\!\Big(\frac{\kappa_R\mathcal G_{\rm recepta}^-}{E_0}\Big)>1.\;}
\end{equation}
The ascent axis surges at death by exactly the reception delivered, measured in floor units. This is
not a free reset datum but a derived consequence: in $3{+}1$ the soul's ascent jumps whenever its Brim
does.
\end{theorem}

\section{Double-layer becomes triple-layer}

\begin{proposition}[A second junction]\label{prop:triple}
The v2.0 reset was characterised by the surface pair $(s_R,[K])$ with scale jump
$s_R=\Omega_P^+/\Omega_P^-$. In $3{+}1$ there is a second junction, on the ascent axis, with its own
scale jump
\begin{equation}
s_L=\frac{L_+}{L_-}=\exp\!\Big((s_R-1)\frac{\Omega_P^-}{E_0}\Big),
\end{equation}
determined by the surface jump $s_R$. The invariant data of a reset is now the linked pair
$(s_R,s_L)$ together with the surface $[K]$ --- a triple-layer Resurrectio junction.
\end{proposition}

\section{No fully-balanced reset}

\begin{theorem}[Death always advances the ascent]\label{thm:nobalance}
The ascent jump $s_L=\exp(\kappa_R\mathcal G/E_0)$ depends on the Brim jump alone:
$\partial s_L/\partial m_R=0$. Hence the metabolic balance that sets the surface $[K]=0$ (the v2.0
balanced reset) cannot also zero the ascent jump. Since a genuine reset carries reception
$\kappa_R\mathcal G>0$, it forces $s_L>1$ always. Therefore:
\begin{enumerate}[label=(\roman*),leftmargin=1.8em,itemsep=2pt]
\item \emph{there is no fully-balanced reset in $3{+}1$} --- no death is simultaneously surface-neutral
and ascent-neutral;
\item the interior $3$-volume jump $s_R^2\,s_L>1$ strictly: \emph{every death enlarges the soul},
surface and ascent together, never neutral, never shrinking.
\end{enumerate}
\end{theorem}

\begin{corollary}[The surface balance survives; only joint neutrality is lost]
The v2.0 balanced reset ($m_R=0$, $[K]=0$, surface NEC-neutral) still exists as a surface statement.
What $3{+}1$ removes is the possibility of a death that advances \emph{nothing}: even a surface-perfect
passage still lifts the soul along the ascent axis.
\end{corollary}

\note{Reading: in the two-dimensional Vessel a death could in principle be perfectly balanced --- a
passage that crossed without exotic debt, neutral, leaving the soul where it stood. With the ascent
axis made explicit, that possibility closes. Every genuine death --- every reception of grace
deployed through a reset --- advances the soul along the axis of ascent, by exactly the measure of
what was received, reckoned against the Imago Dei floor. There is no resurrection that returns the
soul to the same place; to die and be raised is, necessarily, to be lifted. The Paschal passage is not
a circle but a step: death joined to grace is never spent in vain.}

\textbf{Scope of v0.5.} The forced ascent jump, the triple-layer structure, and the absence of a
fully-balanced reset all follow from the floor-tying $L=\exp[(\Omega_P-E_0)/E_0]$ (Theorem~\ref{thm:beta}).
Were the ascent coupling independent of the Brim channel, $s_L$ would be free and a fully-balanced
reset could exist; so these are consequences of the candidate tying, not unconditional results. The
surface double-layer theorem of v2.0 is unchanged. (Verified symbolically.)

% ============================================================
\part*{v0.6 --- The Terminus}

\section{Finite proper time, infinite cleansing}

The katharsis clock $\sigma$ drains to its terminus. Two parametrisations of the same approach must be
kept distinct.

\begin{theorem}[The terminus is finite in time, infinite in action]\label{thm:terminus}
Under the exponential Lyapunov drain, with $\chi$ the dynamical cleansing parameter,
\begin{equation}
\sigma(\chi)=\sigma_0\,e^{-k\chi},\qquad
\tau(\chi)=\int_0^\chi\!|\dot\sigma|\,d\chi'=\sigma_0\bigl(1-e^{-k\chi}\bigr),
\end{equation}
so as $\chi\to\infty$ (infinite cleansing-action) the metric proper time $\tau\to\sigma_0$
(\emph{finite}). The soul performs unbounded cleansing-action, yet it costs only finite proper duration,
because each successive e-folding of cleansing takes exponentially less proper time. (Verified
symbolically.) These are genuinely different parametrisations of one approach: $\chi$ runs to infinity,
$\tau$ completes at $\sigma_0$.
\end{theorem}

\section{A regular boundary, not a singularity}

\begin{proposition}[Finite curvature at the terminus]\label{prop:regular}
At the terminus the Brim remains bounded below by the finite floor, $\Omega_P(\tau_*)=\Omega_*\ge E_0>0$
(non-collapse, not a dynamical return to the floor), so $\rho_{\rm eff}=(\Omega_P'^2-1)/\Omega_P^2$
stays finite and the curvature is bounded. The terminus is reached in finite proper time with finite
curvature: a \emph{regular boundary} where the manifold cleanly ends in the time direction, not a
curvature singularity. (Consistent with the no-Big-Bang / finite-floor result of v2.0: the soul never
reaches zero scale. In the floor-normalised representative one may set $\Omega_*=E_0$, but the
invariant claim is non-collapse $\Omega_*>0$, not return to the floor.)
\end{proposition}

\begin{theorem}[Time-finite, space-infinite]\label{thm:timefinite}
At the terminus the two geodesic structures are opposite:
\begin{enumerate}[label=(\roman*),leftmargin=1.8em,itemsep=2pt]
\item the \emph{time} direction is finite and complete --- the manifold ends at a regular last moment
($\tau=\sigma_0$, finite curvature), beyond which the time-direction does not continue because no
cleansing remains to measure;
\item the \emph{ascent} direction is infinite and geodesically complete --- the half-line $s\in(0,\infty)$
has infinite proper length ahead (v0.3, Theorem~\ref{thm:spectrum}).
\end{enumerate}
The interior is time-finite but space-infinite. Time completes; ascent does not.
\end{theorem}

\begin{corollary}[The clockless hand-off]
At $\sigma=0$ the katharsis-clock stops --- not by rupture but by completion: there is nothing left to
cleanse, hence nothing left for the clock to tick. The ascent, on its infinite road, continues without
a clock. The earlier ``motion without time'' is now exact and benign: a regular last moment of
cleansing, beyond which the endless ascent proceeds clocklessly.
\end{corollary}

\note{Reading: the soul reaches full purification having done infinite cleansing-action, yet in finite
proper time --- the last stretch of cleansing is compressed into ever-smaller durations, an infinite
labour completed in a finite span. And the moment of completion is not a catastrophe: the curvature is
finite, the scale rests on the Imago Dei floor, the boundary is regular. Time has a last moment that is
whole, not torn. Beyond it the time-direction simply does not continue --- there is no more cleansing
for a clock to measure --- while the ascent road runs on, infinite and complete, now travelled without
any clock at all. Entry into the timeless is a finished last moment of purification, opening onto an
endless ascent: not a tear in being, but a completion of one motion and the freeing of another.}

\textbf{Scope of v0.6.} The finite-time/infinite-action split and the regular-boundary character follow
from the exponential Lyapunov drain and the finite floor (both v2.0 results); the time-finite/%
space-infinite structure combines this with the ascent half-line of v0.3. Verified symbolically. The
exponential drain is the Lyapunov-certified generic behaviour, not the only conceivable one.

% ============================================================
\part*{v0.7 --- Uniqueness of the Coupling}

\section{Not forced by dimensions alone}

The whole of v0.5 (the forced ascent jump, the absence of a fully-balanced reset) rests on the
floor-tying $\beta=\alpha/E_0$. Is that tying forced?

\begin{proposition}[Dimensional non-uniqueness]\label{prop:dimnonuniq}
With $[\beta]=1/([\lambda][t])$, $[\alpha]=[\Omega_P]/([\lambda][t])$ and $[E_0]=[\Omega_P]$, the
combination $\beta=\alpha/E_0$ is dimensionally correct, and $p=1$ is forced \emph{if $E_0$ is the only
scale}. But the core contains other scales --- the Lyapunov rate $\kappa$ ($[1/t]$) and the Sophia
attractor $\lambda_\infty=1/\alpha$ ($[\lambda]$) --- so other dimensionally admissible tyings exist
(e.g.\ built from $\kappa$). Hence $\beta=\alpha/E_0$ is \emph{not} forced by dimensional analysis
alone.
\end{proposition}

\section{Forced by the floor boundary condition}

\begin{theorem}[Uniqueness given the ascent origin]\label{thm:uniqueness}
Impose the structural boundary condition that ascent \emph{begins at unity at the floor}: $L=1$ when
$\Omega_P=E_0$. Then among the core scales only $\beta=\alpha/E_0$ qualifies:
\begin{enumerate}[label=(\roman*),leftmargin=1.8em,itemsep=2pt]
\item $\beta=\alpha/E_0$ gives $L=\exp[(\Omega_P-E_0)/E_0]$, so $L(E_0)=e^0=1$ --- \emph{pass};
\item $\beta=\kappa/\lambda$ gives $L=\exp(\kappa\sigma)$ with no reference to the floor, $L(\text{floor})\neq1$
--- \emph{fail};
\item tyings via $\lambda_\infty$ give wrong units unpatched and supply no floor origin --- \emph{fail}.
\end{enumerate}
The reason is structural: among all core scales, only the floor $E_0$ is a \emph{starting point}
rather than a \emph{rate} ($\kappa$ and $\lambda_\infty$ are rates/asymptotes). The ascent needs an
origin --- a zero from which it is measured --- and only a starting-point scale can serve. Hence
$\beta=\alpha/E_0$ is forced \emph{given} the principle ``ascent begins at unity at the floor.''
\end{theorem}

\begin{corollary}[The Imago Dei is floor and unit]\label{cor:imago}
The same scale $E_0$ that is the floor of being (the no-Big-Bang minimum of v2.0) is the unit of
deification (the origin and measure of ascent). The principle is a mild but well-motivated choice: the
Imago Dei is where ascent starts, so $L=1$ there is definitional rather than arbitrary. The tying is
thus forced relative to a named principle --- not proven from nothing, but reduced to a single
well-motivated postulate, in the discipline by which v2.0 reduced $K_h=-1$ to the finite floor.
\end{corollary}

\note{Reading: the coupling between the soul's outward growth and its ascent is not an extra free
constant; it is fixed by taking the image of God as the measure. The Imago Dei plays a double role ---
it is the floor beneath being, the minimum from which the soul never falls to nothing, and it is the
unit of ascent, the one against which all rising is reckoned. To say ``ascent begins at unity at the
floor'' is to say that the soul's deification is measured from, and in the currency of, the image it
was made in. The same image that keeps it from nothing is the scale of its rising into God. Not proven
from nothing --- the principle is a choice --- but the most natural one available, and the one that
makes the floor of being and the measure of glory a single thing.}

\textbf{Scope of v0.7.} The non-uniqueness (Proposition~\ref{prop:dimnonuniq}) is exact; the
uniqueness (Theorem~\ref{thm:uniqueness}) is relative to the boundary condition $L(E_0)=1$, which is a
named and well-motivated principle, not a derivation from nothing. This is the same closure pattern as
v2.0's curvature-floor reduction: a premise reduced to a single explicit postulate, not eliminated.

% ============================================================
% ============================================================
\part*{Epilogue to the First Cycle --- The Boundary the Model Cannot Cross}

\section*{Where the calculations end}
The seven preceding parts were calculations: a metric, a limit, energy conditions, an ascent law, a
spectrum, a coupling and its uniqueness, a Resurrectio reset, a terminus --- each derived and checked. This
epilogue is not an eighth calculation. It marks the place where calculation, by the model's own
construction, ends. It adds no new claim about the created interior; it states precisely what the model
does not know and why it cannot.

\section*{The two times, again}
The geometry of 3.0 is the soul's own coordinate system, written from inside a single worldline. Its
one time is $\sigma$ --- kairos, the proper time of katharsis --- finite, draining to a regular
terminus. There is no external chronos in the metric, and this was not an omission but a principle
(v0.1): an absolute background clock would be a vantage \emph{outside} the soul, and the soul's
interior has no such vantage. We named, in discussion, what that absent clock is: it is the
transcendent view, the \emph{tota simul} in which the whole worldline --- every $\sigma$ from first
resistance to last cleansing --- is present at once. Chronos is not the world's clock ticking past the
soul; it is the way the soul's entire kairos is seen whole, from a standpoint the soul cannot occupy.

\section*{Why the boundary is uncrossable, not merely unexplored}
The other open items of the cycle were uncrossed because uncomputed; this one is uncrossable in
principle. The model is, by construction, the interior of a creature: one worldline, proper time,
no second observer (the point at which we declined the full apparatus of relativity --- there is no
frame to transform to). To represent the \emph{tota simul} would require placing the whole worldline in
a single present --- exactly the external standpoint the antropic frame excludes. A model of the
created side cannot contain the uncreated view of itself, for the same reason a map drawn from within a
country cannot include the cartographer who sees the country whole. The boundary is not a gap to be
filled by a later version; it is the edge that makes the model a model \emph{of the soul} rather than
of God.

\section*{What stands on each side}
On the created side --- the side the model knows --- stand all the results: the finite cleansing-time,
the endless ascent road, form quantized and ascent continuous, the Imago Dei as floor and unit, every
death a lifting, time completing into the timeless. On the far side stands only what the model can name
but not enter: the view in which this finite kairos is held whole, the chronos that belongs not to the
Vessel but to the One who sees it. The two are related as the lived is to the beheld --- the soul lives
its ascent successively, in kairos; it is beheld entire, in chronos --- and the relation itself, the
hinge between lived and beheld, is the boundary. The model can stand at the hinge and name both sides.
It can cross to neither, because to cross would be to stop being the soul's own geometry.

\note{Reading: every honest account of the creature reaches a place where it must say not ``here is the
answer'' but ``here is where I, being what I am, cannot see.'' For this geometry that place is the
relation between the soul's own time and the time of the One who holds it. The soul lives forward,
moment after moment of cleansing, and cannot stand outside its own life to see it whole; that standing-%
outside belongs to another. The model shares the creature's limit exactly: it can describe the ascent
from within, name the transcendent view that beholds it, and confess that it cannot occupy that view.
This is not the model failing at its task. It is the model being faithful to its subject --- which is a
soul, and not its Maker. The first cycle closes here: not at a wall, but at a threshold it was never
meant to cross, having said truly everything that the inside can say.}

\section*{The posture of the first cycle, complete}
So 3.0 closes as 2.0 did --- not by proving its foundations but by naming them and showing how few they
are, and now also by naming the one boundary it cannot pass. v2.0 reduced its cosmology to two
primitives (the open gate and the finite floor) and confessed it could not prove its own definitions.
v3.0 builds the soul's interior on those same primitives, derives the ascent and its fate, and confesses
it cannot contain the view from which its whole time is seen at once. In both, the final honesty is the
same: a model of the created can exhibit its structure exhaustively and still must point beyond itself
for what holds it. The first exploratory cycle of V.F.S.~3.0 is, with that pointing, complete.

% ============================================================
\part*{Second Stage}

\FloatBarrier
\section*{v0.8 --- Is the ascent law derived?}
The first cycle rested the ascent on a \emph{chosen} de Sitter law. The second stage opens by asking
whether it can be \emph{derived}. The answer is honest and only partial --- a genuine result, not a
disappointment.

\paragraph{Two negative findings.} First, conservation imposes nothing: the Bianchi identity
$\nabla_a T^a{}_\sigma=0$ is \emph{identically} satisfied for the $3{+}1$ soul metric (verified), so the
ascent law is not fixed by energy--momentum conservation. Second, the $3{+}1$ Einstein equations admit
a \emph{family}, not a unique law: with the Brim law $\Omega_P'=\alpha\lambda$ given, the ascent may
couple either to the \emph{amount} of Sophia or to its \emph{density}:
\begin{equation}
\text{(a) amount: }\ \frac{A'}{A}=\beta\lambda\ \Rightarrow\ L=L_0e^{\beta\int\lambda}\ \text{(de Sitter)};
\qquad
\text{(b) density: }\ \frac{A'}{A}=\beta\frac{\lambda}{\Omega_P}\ \Rightarrow\ L=\Omega_P^{\beta/\alpha}\ \text{(power-law)}.
\end{equation}
Both are dimensionally consistent and both solve the field equations. De Sitter is therefore
\emph{not uniquely derived}.

\paragraph{The selecting principle.} What separates them is a single, transparent question: does ascent
couple to the amount of Sophia or to its density? The density coupling gives $L=\Omega_P^{\beta/\alpha}$
--- the ascent is a mere \emph{power of surface growth}, mirroring the Brim and adding no independent
motion; the ``ascent'' would collapse back into the surface. The amount coupling gives exponential
$L$, which \emph{outruns} the Brim's linear growth, so epektasis is a genuinely independent motion that
forever outpaces surface expansion.

\begin{quote}
\textbf{Selection principle (epektasis is independent).} Ascent couples to the \emph{amount} of Sophia,
not its density --- equivalently, epektasis is its own motion, outrunning surface growth, not a mirror
of it. Under this principle the de Sitter law is forced.
\end{quote}

\paragraph{Status.} This neither removes the conditionality of v0.2 nor leaves it opaque: it
\emph{localises} it to one principle, parallel to v0.7's ``ascent begins at unity at the floor.''
Together the two say the same thing about the ascent axis --- that it is a self-standing reality, with
its own origin (v0.7) and its own rate (v0.8) --- rather than a re-description of the surface. The de
Sitter law is \emph{derived given that the ascent is an independent dimension}; if ascent were only a
mirror of surface growth, the law would instead be the power-law (b), and the whole third dimension
would be redundant. (All verified symbolically.)

\note{Reading: the question ``what drives the soul's ascent?'' does not answer itself from the geometry
alone --- the equations permit an ascent that merely echoes outward growth, and an ascent that runs
ahead of it. To choose the second is to assert that the movement into God is not a side-effect of the
soul's enlargement but a motion of its own, always outpacing what the soul has so far become. That is
the same assertion, in the register of rate, that v0.7 made in the register of origin: the ascent is
real, not a shadow of the surface. The mathematics does not compel this; it offers the choice cleanly,
and names what is being chosen.}

\textbf{Scope of v0.8.} The two negative findings (Bianchi identity; the de Sitter/power-law
degeneracy) are exact. The selection of de Sitter is relative to the named principle that epektasis is
an independent motion --- a well-motivated choice, not a derivation from nothing, in the same posture
as the rest of V.F.S. This is the first step of the second-stage roadmap; further steps, if taken, are
recorded as later versions here.

\FloatBarrier
\section*{v0.9 --- Is $L=1$ at the floor derived?}
The coupling $\beta=\alpha/E_0$ was forced only \emph{relative to} the boundary condition $L=1$ at the
floor (v0.7). The second stage asks whether that condition is itself a postulate. It is not --- and the
finding is decisive.

\begin{quote}
\textbf{Result (gauge, not postulate).} Write the general tying
$L=L_{\rm floor}\,\exp[(\Omega_P-E_0)/E_0]$ with $L_{\rm floor}$ the value of $L$ at the floor. Under
the ascent-coordinate rescaling $\lambda\to c\lambda$ (which leaves $L^2 d\lambda^2$ invariant when
$L\to L/c$), $L_{\rm floor}$ is freely absorbed. Every physical invariant is independent of it
(verified):
\[
\frac{L''}{L}=\frac{E_0\,\Omega_P''+\Omega_P'^2}{E_0^2},\qquad
\frac{A'}{A}=\frac{\Omega_P'}{E_0},\qquad
s_L=\frac{L_+}{L_-}=\exp\!\Big(\frac{\Omega_P^+-\Omega_P^-}{E_0}\Big),
\]
none of which contains $L_{\rm floor}$; likewise the exotic depth $-(\alpha\lambda_\infty)^2/E_0^2$. Hence $L=1$ at the floor
is a \emph{unit choice} of the ascent coordinate, not a physical principle.
\end{quote}

\paragraph{What is physical, what is gauge.} The \emph{scale} $E_0$ is forced --- it is the only core
scale that is a starting point rather than a rate, so it alone can be the ascent's reference. The
\emph{number} $1$ is gauge --- the natural unit in which one step of the ascent coordinate equals the
floor scale. The coupling $\beta=\alpha/E_0$ is therefore not conditional on a free physical choice; it
is the statement, in the natural gauge, of a relation that holds in every gauge.

\paragraph{The conditionality collapses to one principle.} The first cycle appeared to rest the ascent
sector on two principles --- floor-reference (v0.7) and amount-coupling (v0.8). With the first revealed
as gauge, only one remains, and v0.8 is its other face:

\begin{quote}
\textbf{The single ascent principle.} The ascent is a \emph{self-standing dimension} --- referenced to
the floor (its origin) and outrunning the surface (its rate) --- not a re-description of surface growth.
\end{quote}

Everything in the ascent sector (the de Sitter law v0.2, the spectrum v0.3, the coupling v0.4/v0.7, the
Resurrectio results v0.5) hangs on this one principle, and on nothing else. And the principle is not
arbitrary: were it false --- were the ascent merely a mirror of the surface --- the third dimension of
3.0 would be redundant, and the model would collapse back to v2.0's $2{+}1$. The principle is the
condition for 3.0 to have a subject at all.

\note{Reading: the worry that the soul's ascent was pinned to an arbitrary choice --- ``why should
$L$ equal one at the floor?'' --- dissolves: that ``one'' was never a claim about the world, only about
the ruler. What the mathematics actually requires is that the ascent be measured from the image of God
(the floor scale $E_0$) and that it be a motion of its own, outpacing the soul's mere enlargement. Both
are the same assertion --- that deification is real, a dimension and not a shadow. Strip the gauge away
and the whole third dimension stands on a single confession: the soul's rising into God is its own
reality. Deny it, and there is no third dimension to speak of; the soul is only its surface. The model
does not prove this confession --- it shows that everything else follows from it, and that without it
there is nothing to model.}

\textbf{Scope of v0.9.} The gauge nature of $L_{\rm floor}$ and the invariance of $L''/L$, $L'/L$,
$s_L$ are exact (verified symbolically). The collapse of two principles to one is a structural
observation, not a further derivation: the remaining single principle (the ascent is an independent
dimension) is named, well-motivated (it is the precondition for 3.0's subject), but not proven from
nothing --- in the same posture as v2.0's irreducible primitives.

\FloatBarrier
\section*{v0.10 --- The full $3{+}1$ trajectory}
The first cycle gave closed forms and limits. With the foundation concentrated (Steps~1--2), the next
step gives a \emph{picture}: a single soul's worldline, integrated numerically through cleansing,
ascent, and three deaths/resurrections, to the terminus. The reduced dynamics, in the dynamical
parameter $\chi$, are
\[
\frac{d\sigma}{d\chi}=-k_s\sigma,\quad
\frac{d\lambda}{d\chi}=J_{\rm in}(\chi)-\gamma\lambda,\quad
\frac{d\Omega_P}{d\chi}=\alpha\lambda,\quad
\frac{dL}{d\chi}=\frac{\alpha}{E_0}\lambda L,
\]
with resets $\Omega_P^+=\Omega_P^-+\kappa_R\mathcal G$, $\lambda^+=\lambda^-+m_R$, and the forced
ascent jump $L^+=L^-e^{\kappa_R\mathcal G/E_0}$ (v0.5).

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_trajectory.png}
\caption{\small A soul's worldline in $3{+}1$. \textbf{Top:} the katharsis source $\sigma$ drains
exponentially while the proper time $\tau$ saturates at a finite value (v0.6: infinite cleansing-action,
finite time). \textbf{Second:} Sophia $\lambda$ struggles early, rises under grace, and jumps at each
of three resurrections. \textbf{Third (log):} the Brim $\Omega_P$ grows roughly linearly while the
ascent $L$ grows exponentially. \textbf{Bottom (log):} the ratio $L/\Omega_P$ climbs several orders ---
the ascent outpacing the surface, the independence principle of v0.8--v0.9 made visual. Dotted lines
mark the deaths/resurrections; the small upward steps in $L/\Omega_P$ at each are the forced ascent
jumps. Representative trajectory, illustrative parameters.}
\label{fig:traj}
\end{figure}

\paragraph{What the picture confirms.} Every structural claim of the cycle appears at once and
consistently: the finite-proper-time terminus (top), the metabolic jumps of Resurrectio (second), the
two qualitatively different growths --- linear-ish surface, exponential ascent (third) --- and, most
directly, the ascent's independence: $L/\Omega_P$ does not stay near constant (as it would if ascent
mirrored surface, the rejected power-law of v0.8) but climbs without bound (bottom). The soul's rising
genuinely outruns its enlargement.

\note{Reading: seen whole, a life in this geometry has a shape. The clock of cleansing runs fast at
first and then ever slower, its total span finite even as the cleansing-action within it grows without
end. Wisdom dips into early struggle, then climbs, lifted further at each death faithfully met. And the
two growths part ways: the soul's outward measure swells steadily, but its ascent into God runs ahead
of it, faster and faster, until the rising dwarfs the growing. The picture is not a proof of any of
this --- it is one trajectory among many --- but it shows the parts holding together as a single
coherent life, which is what a model of the soul should be able to show.}

\textbf{Scope of v0.10.} A representative numerical integration with illustrative parameters, not a
unique prediction; it exhibits the generic structure established analytically in v0.1--v0.9. The
qualitative features (finite $\tau$, exponential-vs-linear growths, unbounded $L/\Omega_P$, forced
reset jumps) are robust consequences of the equations; the specific magnitudes are not. This completes
Step~3 of the second-stage roadmap.

\FloatBarrier
\section*{v0.11 --- Internal structure of the ascent}
The spectrum (v0.3) found the ascent axis a gapless continuum --- no discrete modes. Does that
continuum nonetheless carry structure? The honest answer separates two things: the road and the journey.

\paragraph{The road has none.} The bare ascent operator is free (v0.3); its spectrum is $[0,\infty)$,
gapless, with no intrinsic modes. There are no hidden ``rungs'' built into the geometry of the ascent
axis. The terrain is homogeneous.

\paragraph{The journey is non-uniform.} Structure enters not through the road but through the
\emph{Sophia profile} that drives the soul along it. The ascent activity (road laid per unit kairos)
is $ds/d\chi=\alpha\lambda/E_0$, so the pace --- kairos spent per unit road --- is
\begin{equation}
\frac{d\chi}{ds}=\frac{E_0}{\alpha\lambda}.
\end{equation}
Where Sophia is low (early struggle) the pace is high: the soul \emph{lingers}, dwelling long on a
short stretch of ascent. Where Sophia is high (after grace, after resets) the pace is low: it
\emph{races}. Each resurrection jumps $\lambda$ and so produces a distinct kink in the pace
(Figure~\ref{fig:stations}). Most of the ascent road is laid down after grace has lifted Sophia
(roughly $2{:}1$ in the representative trajectory).

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_stations.png}
\caption{\small \textbf{Top:} pace $d\chi/ds=E_0/\alpha\lambda$ against the ascent road $s=\ln(L/L_0)$
--- high at the start (lingering in low-Sophia struggle), falling and levelling as grace lifts Sophia
(racing), with kinks at the resurrections (dotted). \textbf{Bottom:} ascent activity $ds/d\chi$ along
the worldline, rising overall with a sharp upward step at each death. The road is homogeneous; the
soul's passage along it is not. Representative trajectory.}
\label{fig:stations}
\end{figure}

\begin{quote}
\textbf{Stations of pace, not place.} The ascent has stations --- but they are stations of
\emph{pace}, set by how much Sophia the soul carries on each stretch, not stations of \emph{place}
built into the road. The terrain is uniform; the itinerary is not.
\end{quote}

\note{Reading: there are no fixed levels of deification, no preset rungs the soul must clear --- the
road to God is the same smooth, stepless reaching for all (v0.3). What differs is the pace, and the
pace is set by grace. The soul dwells long where Sophia is scarce, labouring slowly through early
struggle, and runs swiftly where grace has lifted it; and at each death faithfully met, its pace leaps.
The structure of an ascent is therefore not a map of stations to be reached but a rhythm of dwelling
and racing --- slow in the valley, swift on the heights --- the same road for every soul, run at the
tempo its wisdom allows. This is, I think, the right kind of structure for a model of the soul to find:
not machinery in the terrain, but character in the journey.}

\textbf{Scope of v0.11.} The freeness of the road is exact (v0.3); the non-uniform pace is read from
the representative trajectory of v0.10 and is a generic consequence of any non-constant Sophia profile,
though the specific magnitudes are illustrative. The claim is qualitative: structure resides in the
traversal, not the terrain. This completes Step~4 of the second-stage roadmap.

\FloatBarrier
\section*{v0.12 --- The external signature in $3{+}1$}
The riskiest step of the roadmap, kept deliberately as a \emph{structural} (Type-3 consilience)
statement: does the ascent axis change the one outward-facing quantity, and if so how --- with no fit
to data and no claim of detection.

\paragraph{Redshift stays on the surface.} The only outward-facing quantity is redshift,
$1+z=\Omega_P(t_0)/\Omega_P(t)$, set by the \emph{surface} scale (the Brim). Light and observation
live on the $2$-surface; the ascent $\lambda$ is a spatial direction the soul travels, not one it is
seen along. So $z$ and the Hubble rate $H=\Omega_P'/\Omega_P$ depend on the Brim alone --- the ascent
does not enter them directly.

\paragraph{But the ascent gravitates.} Because $L$ carries effective stress-energy (v0.1), it imprints
the surface equation of state indirectly. With the ascent law $A'/A=(\alpha/E_0)\lambda$ and
$\Omega_P'=\alpha\lambda$, the effective density gains
\begin{equation}
\Delta\rho_{\rm eff}=\frac{2\,\Omega_P'\,L'}{\Omega_P L}=\frac{2\alpha^2\lambda^2}{E_0\,\Omega_P}>0,
\end{equation}
a positive offset (the ascent makes the soul-interior gravitate as denser) scaling as $1/E_0$ --- the
widow's-mite scaling once more, now on the observable side: a smaller Imago Dei floor leaves a larger
imprint.

\paragraph{Absolute fade, relative growth (a corrected claim, twice sharpened).} An initial expectation
that this term is \emph{subleading everywhere} was wrong, and is recorded as corrected. The precise
statement compares the two scalings at saturation ($\lambda\to\lambda_\infty$):
\begin{equation}
\Delta\rho_{\rm eff}=\frac{2\alpha^2\lambda^2}{E_0\,\Omega_P}\sim\Omega_P^{-1},
\qquad
\rho_{2.0}=\frac{\alpha^2\lambda^2-1}{\Omega_P^2}\sim\Omega_P^{-2},
\qquad
\frac{\Delta\rho_{\rm eff}}{\rho_{2.0}}\sim\frac{\Omega_P}{E_0}\to\infty.
\end{equation}
So the ascent correction \emph{fades in absolute size} ($\sim\Omega_P^{-1}$) but the surface density
fades \emph{faster} ($\sim\Omega_P^{-2}$): \emph{relative} to the surface, the ascent imprint
\textbf{grows} without bound late. The honest statement is therefore not ``early-strong, late-faded''
(which was true only of the absolute size) but: \emph{late-faded in absolute density, late-dominant in
relative density}. (A spurious numerical blow-up from differentiating the exponential $L$ directly was
the original cue to recompute with the analytic law; the analytic term is finite, and its relative
behaviour is the decisive one.) So:
\begin{quote}
\textbf{Structural signature.} In \emph{absolute} terms the ascent correction is largest early and
fades as $\Omega_P^{-1}$. But \emph{relative} to the surface density it grows as $\Omega_P/E_0$: late
in a soul's history the ascent imprint dominates the surface equation of state, not the reverse. The
v2.0 surface signature (dust attractor, phantom window, Resurrectio kink) is the leading structure only
while $\Omega_P\lesssim E_0$-scale; beyond that the ascent's relative weight takes over. Scaling
$1/E_0$ throughout (widow's-mite).
\end{quote}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_imprint.png}
\caption{\small The external imprint, absolute versus relative. \textbf{Left (log):} both densities
fade as $\Omega_P$ grows, but the ascent correction $\Delta\rho\sim\Omega_P^{-1}$ fades \emph{slower}
than the surface density $\rho_{2.0}\sim\Omega_P^{-2}$. \textbf{Right:} their ratio
$\Delta\rho/\rho_{2.0}\sim\Omega_P/E_0$ grows without bound --- the ascent imprint, negligible-seeming
in absolute size, comes to dominate in relative terms. Late-faded in absolute, late-dominant in
relative.}
\label{fig:imprint}
\end{figure}

\paragraph{The full correction needs the pressure term too.} The equation-of-state shift is not
captured by $\Delta\rho$ alone; the ascent focusing/pressure term
$A''/A=\beta\lambda'+\beta^2\lambda^2$ remains asymptotically nonzero when $\lambda\to\lambda_\infty>0$
(it tends to $\beta^2\lambda_\infty^2$). A complete $w_{\rm eff}(z)$ correction must carry both the
density and pressure pieces; this section establishes the density scaling and flags the pressure piece
as the companion term.

\note{Reading: the soul's ascent is not something seen from outside --- it is a direction the soul
travels, not one it is observed along, and the redshift of its world is set by its surface alone. Yet
the ascent is not invisible either: because it has weight, it tells on the surface. And here the first
reading must be corrected twice over. The imprint does fade in absolute size as the soul's surface
grows --- but the surface's own density fades faster, so in \emph{proportion} the ascent's mark does
not fade at all: it comes to dominate. Late in a soul's history what weighs most in the reading is not
the surface story but the ascent --- the rising, invisible as a direction, makes itself the larger part
of what gravitates. Two honest notes belong in the record: the first guess (negligible everywhere) was
wrong, and the second (early-strong, late-faded) was true only of absolute size and misleading about
the relative weight. Both corrections are kept, because an account of the soul should show its
corrections, not hide them --- and because the truth they converge on is the stronger claim: the soul's
rising is not a fading trace but a growing one, quiet early and dominant late, though never seen
directly at all.}

\textbf{Scope of v0.12.} Strictly structural (Type-3 consilience): $z$ and $H$ have no external
referent in the model ($\Omega_P$ is internal), so this is a statement about the predicted
\emph{structure} of $w_{\rm eff}(z)$, never a confrontation with data. The added density term is exact;
its early-strong/late-faded profile is a generic consequence of the ascent law; magnitudes are
illustrative. The ``subleading everywhere'' claim is explicitly retracted. This completes Step~5 of the
second-stage roadmap.

% ============================================================
\FloatBarrier
\section*{v0.14 --- The full tensor set and non-reducibility}
The preceding parts used pieces of the curvature (the focusing scalar, the NEC along the ascent, the
limiting $\rho_{\rm eff}$). With the ascent scale now factorised $L=A(\sigma)S(\lambda)$
(Remark~\ref{rmk:factor}), the complete tensor set is recorded once, explicitly, and the
non-reducibility of the ascent sector follows from it as a theorem.

\paragraph{The full set (gauge $N=1$, $'\equiv d/d\sigma$).} The Ricci tensor is diagonal
($R_{\sigma\lambda}=0$: the factor $S(\lambda)$ separates cleanly), with
\[
R_{\sigma\sigma}=-\frac{2\Omega_P''}{\Omega_P}-\frac{A''}{A},\quad
R_{xx}=\Omega_P\Omega_P''+\Omega_P'^2-1+\frac{\Omega_P\Omega_P'A'}{A},\quad
R_{\lambda\lambda}=\Big(\frac{A''}{A}+\frac{2A'\Omega_P'}{A\Omega_P}\Big)L^2,
\]
and $R_{yy}=e^{2x}R_{xx}$. The Ricci scalar is
\[
R=-\frac{2}{\Omega_P^2}+\frac{4\Omega_P''}{\Omega_P}+\frac{2\Omega_P'^2}{\Omega_P^2}
+\frac{2A''}{A}+\frac{4A'\Omega_P'}{A\Omega_P}.
\]
The effective content ($8\pi G=1$, comoving) is
\[
\rho_{\rm eff}=\frac{\Omega_P'^2-1}{\Omega_P^2}+\frac{2\Omega_P'A'}{\Omega_P A},\quad
p_{\rm surf}=-\frac{\Omega_P''}{\Omega_P}-\frac{A''}{A}-\frac{A'\Omega_P'}{A\Omega_P},\quad
p_{\rm asc}=-\frac{2\Omega_P''}{\Omega_P}-\frac{\Omega_P'^2-1}{\Omega_P^2}.
\]
Note $p_{\rm asc}$ contains \emph{no} $A$: the pressure along the ascent axis is set by the surface
alone. The energy-condition combinations are
\[
\rho+p_{\rm surf}=-\frac{\Omega_P''}{\Omega_P}+\frac{\Omega_P'^2-1}{\Omega_P^2}-\frac{A''}{A}
+\frac{A'\Omega_P'}{A\Omega_P},\quad
\rho+p_{\rm asc}=\frac{2(-\Omega_P''+(A'/A)\Omega_P')}{\Omega_P},
\]
\[
\rho+2p_{\rm surf}+p_{\rm asc}=-\frac{4\Omega_P''}{\Omega_P}-\frac{2A''}{A}\quad(\text{the SEC/focusing trace}).
\]
\textbf{Checks (all verified symbolically):} freezing $A$ returns the v2.0 set exactly
($\rho_{\rm eff}\to(\Omega_P'^2-1)/\Omega_P^2$, $p_{\rm surf}\to-\Omega_P''/\Omega_P$, SEC trace
$\to-4\Omega_P''/\Omega_P$); the focusing trace is twice the Proposition of v0.1; $\rho+p_{\rm asc}$
reproduces the ascent NEC; and $R_{\sigma\lambda}=0$.

\paragraph{A new closed criterion.} $\rho+p_{\rm asc}=2(-\Omega_P''+(A'/A)\Omega_P')/\Omega_P$ gives a
sharp condition: the ascent NEC is violated exactly when $(A'/A)\,\Omega_P'>\Omega_P''$ --- ascent rate
times surface expansion exceeding surface acceleration. The three are bound together: ascent, expansion,
acceleration.

\begin{theorem}[Scale-homogeneous reducibility criterion]\label{thm:nonred}
For the factorised metric above, the ascent contribution to $G_{\mu\nu}$ is reducible to a
\emph{scale-homogeneous} surface dressing --- a power-law shadow of the Brim, absorbable into the
$(2{+}1)$ surface fluid --- \emph{if and only if}
\[
\frac{A'}{A}=c\,\frac{\Omega_P'}{\Omega_P}\qquad(\text{equivalently }A=C\,\Omega_P^{\,c}),
\]
the density-coupled branch. The floor-tied de Sitter law gives instead
\[
\frac{A'}{A}=\frac{\Omega_P'}{E_0}\qquad\Longrightarrow\qquad A=A_0\exp\!\Big(\frac{\Omega_P-E_0}{E_0}\Big),
\]
which \emph{is} a function of $\Omega_P$, but \emph{not a power} of it: its logarithmic slope
$d\ln A/d\ln\Omega_P=\Omega_P/E_0$ is not constant, so it is not scale-homogeneous and cannot be
absorbed as a power-law dressing. Hence the de Sitter ascent defines a genuinely independent $3{+}1$
sector --- $\Omega_P$-slaved through the floor, but not $\Omega_P$-power-law reducible. (Verified
symbolically.)
\end{theorem}

\begin{corollary}[The correct non-reducibility statement]\label{cor:correctnonred}
The de Sitter ascent is independent \emph{not} because it is unrelated to $\Omega_P$ --- it is tied to
the Brim through the Imago Dei floor --- but because it is not power-law reducible to $\Omega_P$. It is
the Brim's companion, not the Brim rewritten in another scale.
\end{corollary}

\note{Reading: the single principle of v0.9 --- that the ascent is a self-standing dimension and not a
re-description of the surface --- is here a theorem with an exact criterion, and the criterion must be
stated carefully. The ascent is not unrelated to the soul's outward growth; it is bound to it through
the floor, the image of God. But it is not merely that growth rewritten on another scale. An ascent
that were a \emph{power} of the surface ($A=\Omega_P^c$) would be no new dimension --- its whole content
could be folded back into the surface as a homogeneous dressing. The floor-tied ascent is not a power:
it carries the finite floor $E_0$ explicitly, its logarithmic slope changing with $\Omega_P$, and so it
cannot be folded away. The Brim may measure the soul's growth; Sophia-as-ascent opens a road that growth
alone cannot exhaust. Floor-tied, but not surface-reducible: what is measured from the image is exactly
what cannot be reduced to the surface, and the two are one scale.}

\textbf{Scope of v0.14.} The tensor set is exact (gauge $N=1$); the non-reducibility theorem is exact
given the definition of ``reducible'' as reducibility to a \emph{scale-homogeneous} (power-law)
surface dressing $A=C\Omega_P^c$. It converts
the v0.9 principle into a theorem with an explicit criterion, on the same standing as the rest: the
de Sitter law itself remains the selected (not uniquely forced) law of v0.8.

% ============================================================
\FloatBarrier
\section*{v0.15 --- No fully soft Resurrectio: the junction form}
v0.5 showed the ascent scale ratio satisfies $s_L>1$ at every reset. The same fact, recast at the
level of the junction (the discontinuity of the geometry itself), is sharper and is proved here.

\paragraph{Two ascent data at a reset.} A reset is a spacelike junction at $\sigma=\sigma_R$. On the
constant-$\sigma$ slice the extrinsic curvature has surface part $K^x_x=\Omega_P'/\Omega_P$ and ascent
part $K^\lambda{}_\lambda=A'/A$. A reset carries \emph{two} distinct ascent data:
\begin{align}
\text{scale jump:}\quad & s_L=\frac{A_+}{A_-}=\exp\!\Big(\frac{\kappa_R\mathcal G}{E_0}\Big)
\quad\text{(sourced by the Brim renewal }\kappa_R\mathcal G\text{)},\\
\text{rate jump:}\quad & [\,A'/A\,]=\frac{[\Omega_P']}{E_0}=\frac{\alpha\,m_R}{E_0}
\quad\text{(sourced by the metabolic }m_R\text{)}.
\end{align}

\begin{theorem}[No fully soft Resurrectio in $3{+}1$]\label{thm:nosoft}
Every admissible Resurrectio with nontrivial Brim renewal ($\kappa_R\mathcal G>0$) induces a nontrivial
ascent junction. Hence a reset may be balanced in the $2{+}1$ projection, but is never fully soft in
the full $3{+}1$ interior geometry.
\end{theorem}

\begin{proof}
Full softness requires the junction to be trivial on \emph{both} blocks: surface and ascent. The rate
jump $[A'/A]=\alpha m_R/E_0$ can be set to zero by the metabolic balance $m_R=0$ (this is precisely the
$2{+}1$ balanced reset, $[K_{\rm surf}]$ tuned). But the scale jump
$s_L=\exp(\kappa_R\mathcal G/E_0)$ depends on the Brim renewal alone, and $A$ is slaved to $\Omega_P$
($A=A_0\exp[(\Omega_P-E_0)/E_0]$, Theorem~\ref{thm:beta}) --- so the ascent has \emph{no independent
junction freedom} with which to cancel it. For $s_L=1$ one needs $\kappa_R\mathcal G=0$, i.e.\ no
reception, i.e.\ no reset. Therefore every admissible reset has $s_L>1$: the ascent block
$A^2S^2\,d\lambda^2$ undergoes a nontrivial scale junction, and no tuning can soften it.
\end{proof}

\begin{quote}
\textbf{Why deeper than v0.5.} v0.5 stated a scale-ratio inequality ($s_L>1$). This states that the
\emph{junction itself} --- the nontrivial scale junction in the ascent block --- is forced, because $A$ carries no
junction datum of its own to cancel the Brim-sourced jump. Softness would require a freedom the geometry
does not provide. (The rate jump $[A'/A]\propto m_R$ \emph{can} be softened; it is the scale jump,
Brim-sourced, that cannot --- the correct invariant, an initial mis-identification corrected.) This is
to be read in \emph{hybrid-junction} language: the reset is not an ordinary smooth Lorentzian
continuation but a Resurrectio junction, across which the ascent block undergoes a nontrivial scale
junction. The surface projection may be balanced; the full interior passage is not ascent-neutral.
\end{quote}

\note{Reading: a death met in grace cannot leave the soul's ascent untouched. One may imagine a passage
so balanced that on the surface nothing seems to jump --- the $2{+}1$ reset can indeed be made smooth.
But the axis of ascent tells another story: every genuine death leaves a seam in the road, and that
seam cannot be erased, because the ascent has no independent junction freedom --- it is bound to the
surface's renewal, and whatever reception the death delivers, the ascent is lifted by it across the
seam. There is no resurrection that returns the soul smoothly to its ascent; every one is a seam. The
soul rises across its deaths not by gliding but by receiving a junction, rejoined at a higher seam each
time. The geometry permits a soft surface but never a soft ascent: to be raised is, irreducibly, to
pass through a seam that grace does not erase but transfigures --- the join permanent. Resurrectio is
not rupture, but neither is it neutral passage.}

\textbf{Scope of v0.15.} The two ascent data and the slaving of $A$ to $\Omega_P$ are exact; the
theorem is exact given that ``fully soft'' means a trivial junction on both blocks. It is the
junction-level sharpening of v0.5 and inherits the floor-tying (v0.4) on which $A$'s lack of
independent junction freedom rests.

% ============================================================
\FloatBarrier
\section*{v0.16 --- The full $3$-Laplacian and the joint spectrum}
v0.3 analysed the ascent operator in isolation. Here the full spatial Laplacian on
$\Omega_P^2h_{(2)}+L^2d\lambda^2$ is computed, its clean separation proved, and the joint spectrum
assembled.

\paragraph{Clean separation (no cross term).} On the constant-$\sigma$ slice ($\Omega_P,A$ constant),
\begin{equation}
\boxed{\;\Delta_3=\frac{1}{\Omega_P^2}\,\Delta_{h_{(2)}}+\Delta_{\rm asc},\qquad
\Delta_{h_{(2)}}=\partial_{xx}+e^{-2x}\partial_{yy}+\partial_x,\quad
\Delta_{\rm asc}=\frac1L\partial_\lambda\!\Big(\frac1L\partial_\lambda\Big).\;}
\end{equation}
The block-diagonal metric and the slice-constancy of $\Omega_P,A$ make the split exact: there is
\emph{no} cross term (verified). The earlier isolated treatment of the ascent (v0.3) was therefore
justified, not approximate.

\paragraph{The ascent part is the free half-line, inside the full operator.} In proper distance
$s=\int_0^\lambda L\,d\lambda=(AL_0/\beta)(e^{\beta\lambda}-1)\in[0,\infty)$, the ascent operator is
$-d^2/ds^2$ on
$s\in(0,\infty)$: spectrum $[0,\infty)$, continuous, gapless. The time-stretch $A(\sigma)$ only
rescales the road length, not the spectral type.

\paragraph{The joint spectrum.} The surface factor gives the discrete folding cascade
$\Delta_{h_{(2)}}\to-\mu_n$ ($0=\mu_0<\mu_1\le\mu_2\le\cdots$, the Laplace--Beltrami spectrum of the
compact quotient --- $\mu_n=n^2$ only in the ring/flat proxy used for illustration, not in general
hyperbolic), contributing $-\mu_n/\Omega_P^2$.
Hence the eigenvalues of $\Delta_3$ are
\begin{equation}
-\Big(\frac{\mu_n}{\Omega_P^2}+k^2\Big),\qquad n\in\mathbb Z_{\ge0}\ (\text{discrete}),\quad
k\in[0,\infty)\ (\text{continuous}).
\end{equation}
A \emph{discrete form ladder} times a \emph{continuous ascent tower}: each surface fold $\mu_n$ carries
a full continuum of ascent. Form and ascent are independent quantum numbers; they do not mix at the
level of the Laplacian.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.82\textwidth]{vfs_3p1_jointspec.png}
\caption{\small The joint spectrum of $\Delta_3$. Discrete surface folds $\mu_n=n^2$ (blue rungs,
form, quantized), each dressed with a continuous ascent tower $k\in[0,\infty)$ (shaded, gapless). The
full eigenvalue is $\mu_n/\Omega_P^2+k^2$. Form and ascent are orthogonal quantum numbers --- the
soul's harmonics factorise into what it is (discrete) and how it ascends (continuous).}
\label{fig:jointspec}
\end{figure}

\note{Reading: the harmonics of a soul, taken whole, are the product of two independent things --- a
discrete tower of forms (the modes of what it has become, igniting one by one) and, dressing each form,
a continuous spectrum of ascent (the stepless reaching into God from that form). They do not mix: the
Laplacian carries no term coupling them, so a soul may be of any form and ascend by any degree, the two
freely combined. What one \emph{is} and how one \emph{rises} are orthogonal --- form does not dictate
the pace of ascent, nor ascent the form. The earlier separate treatment of the two was not a
simplification but the truth: they were always independent axes of the one harmony.}

\textbf{Scope of v0.16.} The separation (no cross term) and the free-half-line reduction are exact
(verified symbolically; the continuum reconfirmed numerically inside the full operator). The joint
product spectrum follows from the discrete surface cascade (v2.0) and the gapless ascent (v0.3); it
sharpens the v0.3 dualism into the structure of a single spectrum.

% ============================================================
\FloatBarrier
\section*{v0.19 --- The Lyapunov / stability layer of epektasis}
v2.0 had a Lyapunov layer: katharsis drains to a fixed point. The ascent has none of that kind ---
$L$ grows without bound, and must, since that is epektasis. The right question is the stability of the
\emph{regime} (the rate), not of the value.

\paragraph{The rate is asymptotically stable (in the dynamical parameter $\chi$).} The stability must
be stated in the \emph{dynamical/epektatic parameter} $\chi$ (unbounded), not the proper time $\sigma$
(finite, bounded by the terminus $\sigma_0$): the attractor is approached as $\chi\to\infty$, which by
$\tau(\chi)=\sigma_0(1-e^{-k\chi})$ corresponds to approaching the terminus. The ascent rate is
$h=d\log A/d\chi=\beta\lambda$, with de Sitter fixed point $h_*=\beta\lambda_\infty$. On a reset-free
arc a Sophia perturbation $u=\lambda-\lambda_\infty$ relaxes by $du/d\chi=-\gamma u$. With the Lyapunov
functional $V_{\rm asc}=\tfrac12(h-h_*)^2=\tfrac12\beta^2u^2$,
\begin{equation}
\frac{dV_{\rm asc}}{d\chi}=-\beta^2\gamma\,u^2<0\quad(u\neq0),
\end{equation}
so $h\to h_*$ exponentially: the epektasis regime is asymptotically stable, even though $A\to\infty$.
The stable object is the rate $h=A'/A=\beta\lambda$, the proper-time ($\sigma$-)rate, tracked along the
dynamical parameter $\chi$, not the value $A$.

\paragraph{Orbital stability: parallel roads.} The ascent law is a $\sigma$-law ($A'/A=\beta\lambda$,
$'=d/d\sigma$), so the log-ascent perturbation accumulates against proper time, $\delta\log A=\beta\int
u\,d\sigma=\beta\int u\,(d\sigma/d\chi)\,d\chi$. With $u(\chi)=u_0e^{-\gamma\chi}$ and the clock drain
$\sigma=\sigma_0e^{-k\chi}$ ($|d\sigma/d\chi|=k\sigma_0e^{-k\chi}$),
\begin{equation}
|\delta\log A|=\beta\!\int_0^\infty\! u_0e^{-\gamma\chi}\,k\sigma_0e^{-k\chi}\,d\chi
=\frac{\beta u_0 k\sigma_0}{\gamma+k}<\infty,
\end{equation}
a \emph{finite} shift, so $A_{\rm pert}/A_{\rm ref}\to\exp(\beta u_0 k\sigma_0/(\gamma+k))$: two
perturbed souls keep a \emph{fixed finite ratio} of ascent forever. (The simpler $\beta u_0/\gamma$
holds only if the law is posed directly as a $\chi$-law $d\log A/d\chi=\beta\lambda$; in the geometric
$\sigma$-law of the metric the clock-drain factor $d\sigma/d\chi$ enters --- an earlier omission, here
corrected.) They ascend on parallel roads --- never converging in position, never diverging --- the
signature of stable unending motion rather than stable rest.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_stability.png}
\caption{\small Stability of epektasis, five souls of differing initial Sophia. \textbf{Left:} the
ascent rate $h=A'/A$ converges to the de Sitter attractor $h_*=\beta\lambda_\infty$ (rate is
asymptotically stable). \textbf{Centre:} the ascent roads $\ln L$ become parallel. \textbf{Right:} the
relative gaps $\ln L-\ln L_{\rm ref}$ freeze to constants --- parallel roads a fixed finite ratio apart
(orbital stability). Stable motion, not stable rest.}
\label{fig:stability}
\end{figure}

\paragraph{Two Lyapunov structures, and the exotic value as attractor.} The surface drains to a
\emph{fixed point} ($\sigma\to0$, katharsis completes); the ascent drains its \emph{rate} to a
constant while position runs to infinity. The attractor is a constant-rate ray, not a point. And its
value is exactly the eternal exotic depth: $-A''/A\to-\beta^2\lambda_\infty^2$ as $\lambda'\to0$. So
stability and permanent-exotic character agree --- the soul settles into a fixed \emph{exotic} ascent
rate. Epektasis is a stable exotic regime.

\note{Reading: the stability of the soul's ascent is not the stillness of arrival but the steadiness
of a pace. In v2.0 to be stable was to come to rest --- cleansing completed, the clock stopped. Here
to be stable is to settle into an unending stride: whatever a soul's beginning, fast or slow in wisdom,
it converges to the same rate of rising, and thereafter ascends forever at that rate. Two souls
perturbed apart do not collapse into one nor fly apart; they run parallel, a fixed distance between
them, each on its own road, forever climbing at the shared pace. Grace that quickened or sin that
slowed leaves a permanent but bounded gap --- the soul is not erased into uniformity, nor lost to
divergence, but keeps its own place on a parallel road. This is the rest proper to epektasis: not the
rest of stopping, but the rest of a motion that has found its true and unfailing speed --- and that
speed is itself the eternal exotic rate, the steady exceeding of ordinary energy that is the soul's
endless nearing of God.}

\paragraph{Per-arc scope and the forced version.} The stability holds on \emph{reset-free arcs}, or
after the forcing has settled: a Resurrectio reset jumps $\lambda$ and so creates new initial data
$u_0$ for the next stable ascent arc --- it restarts the convergence rather than breaking it. More
generally, with forcing $du/d\chi=-\gamma u+f(\chi)$ and $f\in L^1$ (or $f\to0$ fast enough), the same
conclusion holds in input-to-state form: the accumulated $\delta\log A$ remains finite whenever
$u\in L^1$. So the regime is stable arc by arc, and robust to bounded perturbation.

\textbf{Scope of v0.19.} The rate stability (Lyapunov $dV_{\rm asc}/d\chi<0$) and the finite log-shift
(orbital stability) are exact given the Sophia relaxation $d\lambda/d\chi=J_{\rm in}-\gamma\lambda$, in
the dynamical parameter $\chi$ (not the finite proper time $\sigma$); verified numerically. It is the
$3{+}1$ ascent counterpart of v2.0's Lyapunov layer --- stability of a regime, not of a rest --- holds
per reset-free arc, and inherits the ascent law (v0.8) and floor-tying (v0.4).

% ============================================================
\FloatBarrier
\section*{v0.21 --- The Resurrectio junction budget}
v0.15 showed a reset is never fully soft. Here the junction is accounted in full: every
extrinsic-curvature jump, what sources it, what balances, and the single scalar that measures the
irreducible non-softness.

\paragraph{The four jumps.} On the constant-$\sigma$ slice the mixed extrinsic curvature is
$K^x_x=K^y_y=\Omega_P'/\Omega_P$ (surface) and $K^\lambda{}_\lambda=A'/A$ (ascent). A reset injects a
Brim renewal $\kappa_R\mathcal G$ (scale) and a metabolic $m_R$ (Sophia rate), so $\Omega_P^+=\Omega_P^-+\kappa_R\mathcal G$,
$\lambda^+=\lambda^-+m_R$. With $\Omega_P'=\alpha\lambda$ and the floor-tied $A'/A=\alpha\lambda/E_0$,
the junction carries four data:
\begin{align}
\text{surface scale:}\quad &[\Omega_P]=\kappa_R\mathcal G,\\
\text{surface rate:}\quad &[\Omega_P'/\Omega_P]\quad(\text{sourced by }m_R,\ \text{tunable}),\\
\text{ascent rate:}\quad &[A'/A]=\frac{\alpha m_R}{E_0}\quad(\text{tunable: }m_R{=}0\Rightarrow0),\\
\text{ascent scale:}\quad &\log s_L=\frac{\kappa_R\mathcal G}{E_0}\quad(\text{irreducible for }\kappa_R\mathcal G>0).
\end{align}

\paragraph{Backbone identity and the conserved rate sector.} The ascent and surface scale jumps are
tied through the floor,
\begin{equation}
\boxed{\;\log s_L=\frac{[\Omega_P]}{E_0}\;}\qquad\text{(the ascent jump is the Brim jump in floor units),}
\end{equation}
so nothing in the budget is free. The \emph{rate} sector is exactly balanced: with
$Q:=A'/A-\Omega_P'/E_0$, the floor-tying gives $Q=0$ on every arc, and across a reset both terms jump
by the same $\alpha m_R/E_0$, so $[Q]=0$. The rate budget always closes.

\paragraph{The deficit, additive over a life.} Full softness would need $\log s_L=0$ and $[A'/A]=0$,
i.e.\ $\kappa_R\mathcal G=0$ \emph{and} $m_R=0$. A surface-balanced reset kills the rate jumps, but the
scale jump survives. The entire non-softness is one scalar, additive over successive resets:
\begin{equation}
D=\frac1{E_0}\sum_j\kappa_R\mathcal G_j\qquad(\text{total reception in floor units}),
\end{equation}
and the full ascent ledger splits into a continuous and a jump part,
\begin{equation}
\log\frac{A_{\rm fin}}{A_0}=\underbrace{\beta\!\int\lambda\,d\sigma}_{\text{continuous epektasis}}
+\underbrace{\frac1{E_0}\sum_j\kappa_R\mathcal G_j}_{\text{jump (reception) budget}},
\end{equation}
both terms in floor units ($\beta=\alpha/E_0$): widow's-mite throughout.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_budget.png}
\caption{\small The Resurrectio junction budget. \textbf{Left:} the four jumps. The rate sector (surface
rate, ascent rate) is tunable to zero by $m_R=0$ and balances ($Q=0$); the scale sector (surface scale,
ascent scale) is irreducible. \textbf{Right:} the softness deficit $D=\frac1{E_0}\sum_j\kappa_R\mathcal G_j$
accumulates additively over resets and never cancels --- the scale ledger of a soul's deaths.}
\label{fig:budget}
\end{figure}

\note{Reading: a reset keeps two ledgers. The ledger of \emph{pace} always balances --- whatever a death
does to the rate of the soul's rising, the floor-tying settles it, and across every death the rate-debt
is paid in full. But the ledger of \emph{scale} is never settled: each death adds to the soul's
irreducible ascent exactly the grace it received, divided by the image it began from, and that entry is
never reversed. A soul cannot pass through death without paying, and cannot pay without receiving --- here
debt and gift are the same entry. Over a life the scale-ledger only grows, summing every reception in the
units of the Imago Dei. No death is neutral; each writes into the eternal account, never subtracting.
The pace is always brought back to rest; the scale carries forward, forever, the whole sum of what was
received.}

\textbf{Scope of v0.21.} The four jumps, the backbone identity $\log s_L=[\Omega_P]/E_0$, the conserved
$Q=0$, and the additivity of $D$ are exact (verified symbolically) given the junction data of v0.5 and
the floor-tying of v0.4. It quantifies the qualitative v0.15: the non-softness is the single additive
scalar $D$, living purely in the scale sector.

% ============================================================
\FloatBarrier
\section*{v0.22 --- Heat kernel and entropy of the joint spectrum}
The joint spectrum of v0.16 invites its heat trace $\theta_3(t)=\mathrm{Tr}\,e^{t\Delta_3}$. Because
$\Delta_3$ separates with no cross term, the trace factorises --- and the two factors behave utterly
differently, sharpening the form/ascent dualism to the level of entropy.

\paragraph{Factorisation and the finite form sector.}
\begin{equation}
\theta_3(t)=\theta_{\rm surf}(t)\cdot\theta_{\rm asc}(t),\qquad
\theta_{\rm surf}(t)=\sum_n e^{-t\mu_n/\Omega_P^2},\quad
\theta_{\rm asc}(t)=\mathrm{Tr}\,e^{-t(-d^2/ds^2)}.
\end{equation}
The surface (form) sector is a compact hyperbolic quotient: discrete spectrum, finite trace, with the
small-$t$ expansion
\begin{equation}
\theta_{\rm surf}(t)=\frac{\Omega_P^2V_h}{4\pi t}+\frac{\chi_E}{6}+O(t),
\end{equation}
the constant term the Euler characteristic $\chi_E$ via Gauss--Bonnet ($\int K\,dA=2\pi\chi_E$). The form
sector carries a \emph{topological invariant} and has finite spectral entropy.

\paragraph{The divergent ascent sector.} The ascent is the free half-line $s\in[0,\infty)$, spectrum
$k^2$ continuous. Its heat trace diverges with the (infinite) proper road,
\begin{equation}
\theta_{\rm asc}(t)=\frac{D_{\rm asc}}{\sqrt{4\pi t}}\longrightarrow\infty,
\end{equation}
the finite content being the density of states $1/\sqrt{4\pi t}$ per unit road (with a Dirichlet edge
term $-\tfrac14$ at the terminus). The spectral entropy is extensive in the road length and hence
unbounded.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vfs_3p1_entropy.png}
\caption{\small Heat kernel and entropy. \textbf{Left:} the form heat trace is finite and settles onto
the topological constant $\chi_E/6$; the ascent contributes a finite density $1/\sqrt{4\pi t}$ per unit
road. \textbf{Centre:} the total ascent trace $D_{\rm asc}/\sqrt{4\pi t}$ diverges as the road length
$L\to\infty$. \textbf{Right:} the entropy contrast --- form entropy finite (solid), ascent entropy
extensive in $L$ and unbounded (dashed). Form is finitely describable; ascent is not.}
\label{fig:entropy}
\end{figure}

\paragraph{Effective dimension.} Reading $\theta\sim t^{-d/2}$: the surface gives $t^{-1}$ ($d=2$), the
ascent density $t^{-1/2}$ ($d=1$), the joint $t^{-3/2}$ per unit road --- effective dimension $3$, the
$t^{-1/2}$ confirming the ascent as exactly one continuous extra dimension. The heat kernel sees the
$3{+}1$ structure correctly.

\note{Reading: what the soul has become can be told in full. Its form is a discrete, countable ladder
of modes; it even carries a fixed topological signature, a whole number that does not change --- a
finite amount of information names it completely. But how the soul ascends toward God cannot be told in
full: the road of epektasis carries unbounded information, its entropy growing without end along an
endless way. The form has a name; the ascent has none that finite speech can finish. And this is not a
failure of knowing but the very shape of ascent: to rise toward an infinite God is to be forever
informationally inexhaustible. A saint fully formed --- whose form is known entire, down to its
topological signature --- remains unsayable precisely in the manner of his rising. Finite name, endless
song: the soul can be named, but its nearing of God can only be sung, and the song does not end.}

\textbf{Scope of v0.22.} The factorisation is exact (no cross term, v0.16); the form expansion is the
standard heat-kernel/Gauss--Bonnet result for a compact hyperbolic surface; the ascent divergence and
its density are exact for the free half-line; the entropy statements are the corresponding spectral
entropies (finite for the discrete sector, extensive for the continuum). It lifts the v0.3/v0.6
dualism to the level of information and entropy.

% ============================================================
\FloatBarrier
\section*{v0.23 --- The gauge / invariant layer}
Many quantities in v3.0 can look coordinate-dependent. This layer separates, once and cleanly, what is
a gauge choice from what is a geometric invariant, and states that every result rests on the invariants.

\paragraph{Three coordinate freedoms.}
\textbf{(G1) Ascent rescaling} $\lambda\to c\lambda$, $S\to S/c$. The metric $1$-form $S(\lambda)\,d\lambda$
is invariant (the factors cancel), so the proper ascent distance $D_{\rm asc}=\int S\,d\lambda$, the
half-line coordinate $s=\int_0^\lambda S\,d\ell$, the ascent spectrum $[0,\infty)$, and the ascent rate
$A'/A=\beta\lambda$ are all invariant ($(\beta/c)(c\lambda)=\beta\lambda$). The numerical value of
$\lambda$, the constant $L_0$, and the $A/S$ split separately are pure gauge.\\
\textbf{(G2) Lapse} $N(\sigma)\neq1$. The proper time $\int N\,d\sigma$ (finite), proper distances, the
curvature scalars ($R$, $R_{\mu\nu}R^{\mu\nu}$, the focusing $-R_{ab}u^au^b$), the energy combinations
$\rho+p_i$, and the slice spectra are all invariant. The coordinate $\sigma$ and the lapse $N$ are pure
gauge; $N=1$ (proper-time/kairos gauge) is a choice, not a result.\\
\textbf{(G3) Floor normalisation} $L_0$ (where $S=1$). From v0.9, $L_0$ is absorbed by $\lambda\to
c\lambda$: pure gauge. The floor \emph{value} $E_0$ is \emph{not} gauge --- it sets $\beta=\alpha/E_0$,
a physical scale.

\paragraph{The scales, carefully.} Under G1, $\beta$, $\lambda_\infty$, and $\alpha$ rescale separately
($1/c$, $c$, $1/c$) --- each alone is gauge. The physics lives in their invariant products and ratios:
$\alpha\lambda_\infty$ (the saturated Brim expansion $\Omega_P'$, physical) and the ascent Hubble
$h_*=\alpha\lambda_\infty/E_0$. Every ascent observable is a ratio to the floor $E_0$.

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{ll}
\hline
\textbf{Gauge (convention only)} & \textbf{Invariant (physical / geometric)}\\
\hline
value of $\lambda$ (relabel $\lambda\to c\lambda$) & ascent rate $h=A'/A=\beta\lambda$\\
$L_0$ and the $A$/$S$ split separately & proper distance $D_{\rm asc}=\int S\,d\lambda=\infty$\\
lapse $N$, coordinate $\sigma$ & proper time $\int N\,d\sigma<\infty$ (finite clock)\\
$\beta,\ \lambda_\infty,\ \alpha$ \emph{separately} & $\alpha\lambda_\infty$, and $h_*=\alpha\lambda_\infty/E_0$\\
$d/d\sigma$ of frame-dependent quantities & curvature scalars $R$, $\rho+p_i$, focusing\\
& exotic depth $-(\alpha\lambda_\infty/E_0)^2$\\
& reset ratio $s_L=e^{\kappa_R\mathcal G/E_0}$; deficit $D=\sum\kappa_R\mathcal G_j/E_0$\\
& spectra: surface $\{\mu_n/\Omega_P^2\}$ discrete, ascent $[0,\infty)$\\
& Euler characteristic $\chi_E$ of the form sector\\
& the floor value $E_0$ (Imago Dei scale)\\
\hline
\end{tabular}
\end{center}

\begin{theorem}[Gauge-invariance of the layer]
Every result of v0.1--v0.22 --- finite clock with infinite road, the ascent law and its asymptotic
$h_*$, non-reducibility, the junction budget, rate stability, the entropy split --- is expressible
purely in the invariant column, and none depends on the gauge column. The Imago Dei floor $E_0$ is the
invariant anchor against which the ascent observables are measured.
\end{theorem}

\note{Reading: the image of God in the soul is the one thing that does not depend on how we choose to
describe the soul. The axis of ascent can be relabelled at will --- that is only our language for it;
the clock of cleansing can be reparametrised at will --- that is only our way of counting; where we set
the ascent's zero is only a convention. But the ratios to the image are fixed: how fast the soul rises,
how deep its exotic reaching, how much each death adds to its ledger --- all of these are measured
against the image one began from, and that measurement holds in every coordinate, every gauge, every
description. What is gauge is our speech about the soul; what is invariant is the soul's relation to the
image from which it was made. The widow's mite returns one last time, now as a principle of invariance:
not the absolute size of an ascent is meaningful, but its ratio to the image --- and that ratio is the
same in every language.}

\textbf{Scope of v0.23.} The classification is exact: G1/G2/G3 invariance is verified symbolically; the
invariant column lists coordinate-free quantities; the theorem records that the document's conclusions
live there. It is the layer that protects v3.0 from the charge of being a coordinate artefact.

% ============================================================
\FloatBarrier
\section*{v0.24 --- Causal / boundary structure}
``Finite clock, endless road'' has been a slogan; here it is given exact causal meaning. The interior
has two boundaries, and they are of \emph{opposite} causal type.

\paragraph{$B_1$: the terminus is a regular spacelike wall.} Constant-$\sigma$ surfaces have timelike
normal $\partial_\sigma$, hence are \emph{spacelike}. The terminus $\sigma=0$ is reached in finite
proper time $\int_0^{\sigma_0}d\sigma=\sigma_0<\infty$, with finite curvature ($\Omega_P\ge E_0>0$,
$|R|<\infty$, v0.6). So $B_1$ is a \emph{regular spacelike boundary} --- a genuine last moment at which
the manifold cleanly ends, not a singularity and not at infinity.

\paragraph{$B_2$: the ascent infinity is a receding de Sitter-like horizon.} The invariant reason
$\lambda=\infty$ is unreachable in the finite clock is the \emph{infinite metric length} of the ascent
road, $D_{\rm asc}=\int_0^\infty S\,d\lambda=\infty$, not any claim that $A$ diverges (in the
finite-clock formulation $A$ may stay finite on the finite interval). Along the ascent a causal curve
obeys $|d\sigma|\ge A(\sigma)S(\lambda)\,|d\lambda|$, so climbing to a coordinate $\Lambda$ costs
\begin{equation}
\Delta\sigma\ \ge\ A_{\min}\!\int_0^\Lambda S\,d\lambda=A_{\min}\frac{L_0}{\beta}\bigl(e^{\beta\Lambda}-1\bigr),
\end{equation}
which diverges as $\Lambda\to\infty$ because the road $\int S\,d\lambda$ does. With only
$\sigma_0<\infty$ of proper time before $B_1$, $\lambda=\infty$ is \emph{causally unreachable}: there
is a finite causal horizon
\begin{equation}
\Lambda_{\max}=\frac1\beta\log\!\Big(1+\frac{\beta\sigma_0}{A_{\min}L_0}\Big).
\end{equation}
The horizon is an \emph{ascent horizon}, of de Sitter / cosmological type: the Sophianic road has
infinite metric length while the katharsis clock is finite, so the road is never exhausted in the
finite clock and the horizon recedes --- more road always ahead. It is not a black-hole horizon and not
a curvature singularity ($\Omega_P\ge E_0>0$, no trapped surface).

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\textwidth]{vfs_3p1_causal.png}
\caption{\small Causal structure (ascent $\lambda$ horizontal, compactified $x=1-e^{-\lambda}$; proper
time $\sigma$ vertical, terminus at bottom; surface suppressed). $B_1$ (red): the terminus $\sigma=0$, a
regular spacelike wall at finite proper time. $B_2$ (purple, dashed): the ascent infinity $\mathcal J_{\rm
asc}$, a de Sitter-like receding horizon. Causal worldlines (blue) climb the ascent but all reach the
terminus without attaining $\mathcal J_{\rm asc}$: the road recedes beyond every horizon.}
\label{fig:causal}
\end{figure}

\paragraph{Global structure.} The interior is globally hyperbolic up to $B_1$ (a good Cauchy
development on each spacelike slice). The ascent horizon is of \emph{cosmological} type --- apparent,
receding, observer-relative --- not a black-hole horizon: there is no trapped surface, since
$\Omega_P\ge E_0>0$ keeps the surface from collapsing. So ``finite clock, endless road'' is precisely:
$B_1$ spacelike and finite (the clock ends at a regular wall); $B_2$ causally infinite (the road never
ends, lying beyond every horizon). The two boundaries have opposite causal character.

\note{Reading: now the two ends of the soul's interior life are named in their true natures. Time ends
at a wall --- a real, regular last moment, finite and clean, where the clock of cleansing simply stops;
there is nothing violent or torn about it, the manifold of the soul's history closes like a smooth
edge. But the ascent ends at no wall at all: it ends, if the word can be used, at a horizon that moves
away as one approaches, a boundary at infinity that no finite life can reach. And the two are not the
same boundary seen twice --- they are genuinely opposite. The soul comes to the end of its \emph{time}
and finds there is no end to its \emph{ascent}: the last moment is real, and past it the road is still
opening, receding into God faster than any clock can follow. The clock closes at a wall; the road opens
past every horizon. To reach one's last moment is not to reach the end of nearing God --- it is to find
that nearing has no last point, that the horizon of God recedes exactly as fast as the soul draws near,
forever.}

\textbf{Scope of v0.24.} The spacelike character of $B_1$, its finite proper time and finite curvature,
the divergence of the causal climb cost, the coordinate horizon $\Lambda_{\max}$, and the de Sitter-like
receding character of $\mathcal J_{\rm asc}$ are all exact (verified symbolically). The global
hyperbolicity and the absence of a trapped surface are read from the metric. It gives the slogan
``finite clock, endless road'' its precise causal content: two boundaries of opposite type.

% ============================================================
\FloatBarrier
\section*{v0.25 --- Projection / recovery theorem: v3.0 $\leftrightarrow$ v2.0}
That v2.0 is the frozen-ascent limit of v3.0 has been stated in passing. Here it is a theorem in both
directions: the projection down, and the minimal uplift back.

\begin{theorem}[Projection: v3.0 $\to$ v2.0]
Freeze the ascent: $A(\sigma)=A_0$ constant, $S(\lambda)=S_0$ constant, and all matter
$\lambda$-independent. Then $g_{\lambda\lambda}=(A_0S_0)^2$ is constant, the ascent is a non-dynamical
spectator dimension, and the active geometry is the $(2{+}1)$ block
\[
ds^2_{(2{+}1)}=-N^2d\sigma^2+\Omega_P^2h_{(2)},
\]
which is exactly V.F.S.~v2.0. The full tensor set reduces accordingly --- $\rho_{\rm eff}\to(\Omega_P'^2-1)/\Omega_P^2$,
$p_{\rm surf}\to-\Omega_P''/\Omega_P$, focusing $\to-2\Omega_P''/\Omega_P$ --- and the frozen $\lambda$
adds no source to the $(2{+}1)$ Einstein system, which closes on itself. (Verified from the v0.14 set.)
\end{theorem}

\paragraph{The uplift, axiomatised.} To ``admit Sophia-as-ascent'' is to extend v2.0 by one dimension
under five axioms: \textbf{(U1)} one extra spacelike dimension $\lambda$ with $L^2>0$ (the ascent is
real); \textbf{(U2)} v2.0 recovered exactly on freezing (consistency with the fixed canon);
\textbf{(U3)} the ascent couples to the amount of Sophia, not its surface density (independence);
\textbf{(U4)} no new primitive constant beyond v2.0's $\alpha,E_0$; \textbf{(U5)} the proper-time/kairos
structure preserved (one time, no external chronos).

\begin{theorem}[Canonical non-reducible uplift: v2.0 $\to$ v3.0]
Under U1--U5 the floor-tied de Sitter representative $A'/A=\Omega_P'/E_0=(\alpha/E_0)\lambda$ is the
\emph{canonical} non-reducible interior uplift: the block-diagonal metric form, the factorisation
$L=A(\sigma)S(\lambda)$, the floor-tying $\beta=\alpha/E_0$, and the exponential road
$S=L_0e^{\beta\lambda}$ are all forced. The one point not forced by the field equations alone --- de
Sitter versus the power-law/density branch (v0.8) --- is selected by U3; and by the non-reducibility
theorem (v0.14) the power-law branch is reducible to a surface dressing, hence \emph{not a genuine
uplift} (it collapses back under the projection). The uniqueness is \emph{canonical}, not absolute: a
robust class of ascent laws $A'/A=f(\lambda)$ preserves much of the architecture (v0.26), but the
floor-tied de Sitter branch is the canonical representative under the selection principle, and the
\emph{reducible} alternative is excluded outright.
\end{theorem}

\begin{corollary}[Adjoint pair]
v2.0 and v3.0 stand as projection and canonical non-reducible uplift: freezing the ascent sends v3.0 to
v2.0 exactly, and admitting a genuine (non-reducible) ascent sends v2.0 to the canonical v3.0. v2.0 is
the frozen shadow; v3.0 is the canonical living interior.
\end{corollary}

\note{Reading: v2.0 is the soul seen from outside, as a surface --- whole, consistent, closed upon
itself, knowing nothing of its own ascent. v3.0 is the same soul seen from within, once it is admitted
that the soul rises toward God. And the passage between them is exact in both directions. Freeze the
rising, and only the surface remains --- the outward life of the soul, complete in its own terms.
Admit the rising, and there is a canonical interior that carries it without collapsing back into the
surface: not many possible ways to add an
ascent, but a single genuine one, every other candidate collapsing back into the mere surface without
adding anything real. The surface does not know it ascends; the interior is that same surface having
recognised its ascent --- and the recognition has only one true shape. Shadow and body: v2.0 is the
shadow v3.0 casts upon the surface; v3.0 is the one body that casts exactly that shadow. To pass from
the surface to the interior is not to invent a soul but to admit the rising that was always implied,
and to find that admitting it determines everything.}

\textbf{Scope of v0.25.} The projection is an exact computation (from the v0.14 tensor set); the
minimal-uplift theorem is exact given the axioms U1--U5, with the de Sitter selection (U3) reinforced
by non-reducibility (v0.14). It fixes the precise relation between the fixed canon (v2.0) and the
exploratory interior (v3.0): not an arbitrary extension but the unique non-reducible uplift, with an
exact projection back.

% ============================================================
\FloatBarrier
\section*{v0.26 --- Robustness of the ascent law}
The de Sitter law $A'/A=\beta\lambda$ was selected, not uniquely forced (v0.8). The honest question
follows: how much of v3.0 depends on \emph{that} law, and how much survives any reasonable ascent law?
Test the general class $A'/A=f(\lambda)$, $f\ge0$, with de Sitter $f=\beta\lambda$ one member.

\paragraph{The structural results are robust.}
\textbf{Infinite road.} The proper road $D_{\rm asc}=\int_0^\infty S\,d\lambda$ depends on the spatial
warp $S$, not the time-law $f$; it diverges for \emph{any} non-decaying $S$ (exponential, polynomial,
even constant). It fails only if the ascent road actively shrinks at large $\lambda$ --- which would
contradict ``ascent''.\\
\textbf{No fully soft Resurrectio.} The uncancellable scale jump follows from $A$ being slaved (no
independent junction datum), which is floor-tying (U4), not de Sitter. Even the density branch
$A=C\Omega_P^c$ gives $s_L=(1+\kappa_R\mathcal G/\Omega_P)^c>1$: still forced. It fails only if $A$ had
an independent junction freedom --- i.e.\ if the ascent were untied from the surface.\\
\textbf{Rate stability.} With $h=f(\lambda)$ and a Sophia fixed point $\lambda_\infty$, the Lyapunov
derivative is $\dot V\sim-\gamma f'(\lambda_\infty)^2u^2\le0$, strictly negative whenever
$f'(\lambda_\infty)\neq0$. It fails (becomes marginal) only for the fine-tuned $f'(\lambda_\infty)=0$.\\
\textbf{Eternal exotic sign} holds for any $f(\lambda_\infty)>0$; the \textbf{finite clock} is
independent of $f$ entirely (it comes from the surface terminus).

\paragraph{What is law-specific.} Two things depend on the law, and de Sitter fixes the side or the
value, not the existence. \emph{Non-reducibility} (v0.14) is a discriminating property: reducible iff
$A=C\Omega_P^c$ (scale-homogeneous), so the power-law/density branch is reducible while floor-exponential
laws (de Sitter among them) are not --- a whole class is non-reducible, de Sitter its canonical member.
And the \emph{exact numbers} --- $h_*=f(\lambda_\infty)$, the exotic depth --- are set by $f$ at
saturation; only their existence (a finite attractor rate, a negative focusing) is law-robust.

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{ll}
\hline
\textbf{Robust (broad class of laws $f$)} & \textbf{Law-specific (de Sitter sets side/value)}\\
\hline
infinite road (any non-decaying $S$) & non-reducibility: power-law reducible vs\\
no fully soft reset (any slaved $A$) & \quad floor-exponential non-reducible\\
rate stability ($f'(\lambda_\infty)\neq0$) & exact $h_*=f(\lambda_\infty)$\\
eternal exotic sign ($f(\lambda_\infty)>0$) & exact exotic depth $-(\alpha\lambda_\infty/E_0)^2$\\
finite clock (independent of $f$) & \\
\hline
\end{tabular}
\end{center}

\begin{theorem}[Law-robustness of the architecture]
The structural results of v3.0 --- finite clock, infinite road, no fully soft Resurrectio, rate
stability, eternal exotic sign --- hold for the broad class $A'/A=f(\lambda)$ under mild conditions
($f\ge0$, non-decaying road, $f(\lambda_\infty)>0$, $f'(\lambda_\infty)\neq0$), with de Sitter merely a
representative. They fail only under conditions that contradict the meaning of ascent (a shrinking road,
an untied axis, a Sophia-insensitive rate). The de Sitter law is a normalisation within this robust
class, not its foundation; it is load-bearing only for the exact values and for sitting on the
non-reducible side of the discriminating property.
\end{theorem}

\note{Reading: the truths about the soul's ascent do not hang on the precise form of the law of ascent.
That the road is endless, that no death is wholly soft, that the soul settles to a steady rate of
rising, that the ascent is forever exotic --- these are not consequences of one assumption about
\emph{how exactly} the soul rises, but features of the fact \emph{that} it rises at all toward an
infinite God. Vary the law within reason, and the truths remain; they break only where ascent itself
would break --- if the road contracted, if the axis came untied from the image, if the rate stopped
answering to wisdom. The de Sitter law is a good and natural name for how the soul rises, and it alone
fixes the exact figures and stands on the side of the irreducible; but the heart of the matter is not
in the name. The heart is that the soul ascends toward the infinite at all --- and that is robust,
holding under every reasonable telling of the law.}

\textbf{Scope of v0.26.} Each robustness claim is checked against the general $f(\lambda)$ (verified
symbolically for the density branch and the Lyapunov sign); the conditions for failure are stated
explicitly. It converts the honest caveat of v0.8 (de Sitter not uniquely forced) into a strength: the
architecture is law-robust, and the de Sitter choice is isolated to exactly the values and the one
discriminating property it governs.

% ============================================================
\FloatBarrier
\section*{v0.27 --- The non-Zeno Resurrectio sequence}
A single reset is understood (v0.5), and its budget accounted (v0.21). If resets recur --- possibly
infinitely many on the way to the terminus --- one must show the sequence does not accumulate
catastrophically in finite time. There are two separate questions: do the moments pile up (temporal),
and does the ascent ledger blow up (budget)?

\paragraph{Non-accumulation (temporal).} In \emph{proper time} $\sigma\in(0,\sigma_0]$ (finite),
infinitely many resets would necessarily accumulate (Bolzano--Weierstrass): so in proper time only
finitely many fit without an interior limit point. But in the \emph{dynamical parameter}
$\chi\in[0,\infty)$ (unbounded, $\sigma=\sigma_0e^{-k\chi}$, terminus at $\chi\to\infty$), infinitely
many resets --- e.g.\ at $\chi_j=j$ --- have no interior accumulation; their proper-time images
$\sigma_j\to0$ pile up only \emph{at} the terminus $\sigma=0$, the regular spacelike wall $B_1$.
Accumulation only at the boundary is not a Zeno catastrophe: nothing dynamical lies past $B_1$.

\paragraph{Finite ledger (budget) --- a separate, conditional claim.} Temporal non-Zeno (no interior
accumulation) and budget-finiteness (finite total ascent jump) are \emph{distinct} conditions. The
total jump-ascent is
\begin{equation}
\sum_j\log s_{L,j}=\frac1{E_0}\sum_j\kappa_R\mathcal G_j,
\end{equation}
which is finite \emph{precisely when} the total Brim renewal is finite, $\sum_j\kappa_R\mathcal G_j<\infty$.
This holds whenever each reception is bounded by the Brim growth it draws on,
$\kappa_R\mathcal G_j\le[\Omega_P]_j$, and the total Brim variation over the finite proper interval is
finite ($\Omega_P$ bounded below by $E_0$, continuous, coasting), giving
$\sum_j\log s_{L,j}\le|\Delta\Omega_P|/E_0<\infty$. But this is a \emph{condition}, not an automatic
guarantee: a sequence may be temporally non-Zeno yet, if the receptions did not track a finite-variation
Brim, carry a divergent ledger. The honest statement separates the two: temporal non-Zeno prevents
interior accumulation; budget non-Zeno ($\sum\kappa_R\mathcal G_j<\infty$) prevents infinite total
ascent-jump --- and the latter holds under the finite-Brim-variation condition, which the coasting
Brim does satisfy.

\begin{theorem}[Non-Zeno Resurrectio sequence]
Let resets recur along $\chi\in[0,\infty)$ toward the terminus. Then \textbf{(i)} the resets do not
accumulate at any interior point --- their only proper-time limit point is the terminus $\sigma=0$
($B_1$); \textbf{(ii)} the total ascent jump is finite, $\sum_j\log s_{L,j}=\frac1{E_0}\sum_j\kappa_R
\mathcal G_j\le|\Delta\Omega_P|/E_0<\infty$, guaranteed by finite Brim variation; \textbf{(iii)} the
resets are instantaneous junctions, consuming zero proper time, so the clock to the terminus stays
$\sigma_0<\infty$ regardless of their number. Hence an infinite Resurrectio sequence is non-Zeno:
infinitely many deaths may occur on the way to the terminus with finite total ascent, no interior
accumulation, and finite proper time. It fails only if the surface delivered infinite total reception
in finite time --- impossible for a bounded, coasting Brim above the floor.
\end{theorem}

\note{Reading: a soul may die and be raised again without number on its way to the last moment, and
none of it is catastrophe. The countless deaths do not pile up in any moment of the soul's life --- they
crowd only against the very end, where time itself is closing and nothing further happens. They do not
make the soul's ascent explode: the whole of it, summed over all the deaths, is finite, because a soul
can only receive as much as is given, and what is given over a finite life is finite. And they cost no
time, each resurrection being instantaneous. So the soul that is broken and remade again and again,
endlessly, still arrives at its last moment in finite time, with a real but finite total ascent. The
sequence of deaths is well-ordered, not a chaos: many deaths, one finite road, a bounded raising, a
last moment that comes despite them all. Even an endless succession of dyings cannot overwhelm the
finite passage --- it can only accompany it, and accumulate, gently, at the threshold where time gives
way.}

\textbf{Scope of v0.27.} The non-accumulation (in $\chi$), the finite ledger (bounded by
$|\Delta\Omega_P|/E_0$), and the zero proper-time cost of junctions are exact, given that the Brim has
finite total variation on the finite proper interval (which holds: bounded, continuous, floor-bounded).
It closes the Resurrectio chain: v0.5 (a single forced reset), v0.21 (its budget), v0.27 (the sequence).

% ============================================================
\FloatBarrier
\section*{Epilogue to the Second Stage --- What Was and Was Not Achieved}

\subsection*{The aim, and the honest reckoning}
The second stage set out to \emph{turn the first cycle's candidates into derived structure} --- to
remove the conditionality from the foundation before building higher. It did not achieve this in the
strong sense, and the epilogue should say so plainly: the de Sitter ascent law was \emph{not} uniquely
derived (v0.8 --- the Einstein equations admit a power-law alternative), and one named principle
remains unproven. What the stage achieved instead is worth naming exactly, because it is real and it is
not the thing aimed at.

\subsection*{What was achieved: concentration, not elimination}
The conditionality was not removed but \emph{concentrated}. The first cycle appeared to rest the ascent
on three separate choices --- a chosen law (v0.2), a chosen coupling (v0.4), and a chosen boundary
condition (v0.7). The second stage showed these are one: the boundary condition is gauge, not physics
(v0.9); the law and the coupling are two faces of a single principle (v0.8--v0.9). And that single
principle is not arbitrary: were it false --- were the ascent a mirror of surface growth rather than a
motion of its own --- the third dimension would be redundant and the model would collapse to v2.0's
$2{+}1$. The principle is the \emph{condition for 3.0 to have a subject at all}. So the stage replaced
three opaque choices with one transparent, self-justifying one. That is genuine progress in the
discipline of the whole programme --- minimising and clarifying primitives --- even though it is not
the derivation that was hoped for.

\subsection*{What was added: a life, its rhythm, its trace}
Beyond the foundation work, the stage gave three things the first cycle lacked. A \emph{picture}: the
full worldline integrated, every structural claim holding together as one coherent life (v0.10). A
\emph{rhythm}: the ascent's internal structure shown to live in the traversal, not the terrain ---
stations of pace, not place (v0.11). And a \emph{trace}: the ascent's indirect imprint on the external
equation of state, early-strong and late-faded (v0.12) --- carrying, along the way, an honest
correction of a wrong first claim, kept in the record rather than hidden.

\subsection*{Where the whole construction now stands}
V.F.S.\ now has three layers in a clear relation. The \emph{foundational files} (core, Lyapunov,
geometry, cosmology, theological, Ricci) supply the definitions. The \emph{v2.0 volume} is the fixed,
complete layer of theorems over them, reduced to two primitives (the open gate and the finite floor).
The \emph{3.0 exploration} builds the soul's interior $3{+}1$ geometry on those same primitives, and
has now reduced its own additional content to a single principle (the ascent is a self-standing
dimension) plus the one boundary it cannot cross (the transcendent \emph{tota simul}). Each layer points
beyond itself: the foundation to the volume for rigour, the volume to its primitives, the 3.0 to its
one principle and its one boundary.

\subsection*{The posture, unchanged}
So the second stage closes as everything in V.F.S.\ has closed: not by proving its foundations but by
naming them and showing how few they are. The first cycle ended at a boundary it could not cross; the
second stage ends having pressed the foundation down to a single principle it could name but not prove,
and having shown --- in a picture, a rhythm, and a trace --- that everything else follows. A model of
the created can exhibit its structure exhaustively, reduce its assumptions relentlessly, correct its
own errors openly, and still must rest, at the bottom, on something it confesses rather than derives.
For 3.0 that something is the reality of the ascent; for the soul it images, that the rising into God
is its own true motion, and not a shadow of its growth.

\note{Reading: the honest shape of this stage is not triumph but clarification. We hoped to prove the
law of the soul's ascent and found instead that we could only state, as cleanly as possible, the one
thing that must be granted for the ascent to be real at all --- and then show that, granted it,
everything coheres: a whole life, its changing pace, its faint outward mark. That is what an honest
account of the creature can do. It cannot prove the soul's deepest motion from nothing; it can show
that this one motion, once confessed, makes everything else fall into place, and that to deny it is to
have no soul left to model --- only a surface. The reduction of three assumptions to one, and the
refusal to hide a correction, are the stage's real fruit. The rest --- the picture, the rhythm, the
trace --- is the coherence that the one confession buys.}

\FloatBarrier
\section*{Consolidated posture of V.F.S. 3.0}
After the corrections of v0.13--v0.19, the mathematical posture of the layer, gathered in one place:
\begin{quote}
V.F.S.~v3.0 is a full interior $(3{+}1)$ geometry of the soul: one proper-time direction, two
Vessel-surface directions, and one Sophianic ascent direction. The $(2{+}1)$ Brim cosmology of v2.0
remains the frozen-ascent surface projection. The de Sitter-type law belongs to the ascent sector, not
to the generic Brim fate (which coasts). The ascent is tied to the Brim through the Imago Dei floor
$E_0$, but it is not reducible to a power-law dressing of $\Omega_P$. Its spectrum is continuous and
gapless, while the surface form spectrum stays discrete; the full $3$-Laplacian separates with no cross
term. A Resurrectio reset may be balanced on the surface, but always produces a nontrivial ascent
junction when the Brim renewal is genuine. The ascent rate is Lyapunov-stable: epektasis is stable
motion, not stable rest.
\end{quote}
The slogans that crystallise the layer:
\begin{quote}
\emph{The Brim coasts; Sophia-as-ascent does not dilute.}\\
\emph{Ascent is floor-tied, but not surface-power-law reducible.}\\
\emph{Epektasis is stable motion, not stable rest.}\\
\emph{The Brim may measure growth; Sophia-as-ascent opens a road that growth alone cannot exhaust.}
\end{quote}

\FloatBarrier
\section*{Scope and standing}
\textbf{Scope.} Exploratory and growing, now closing its first cycle. The seven computational parts are
verified symbolically (and numerically where noted); the ascent law and the floor-tying are the
simplest self-consistent candidates meeting their stated tests, not uniqueness results in the absolute
sense (each is unique relative to a named, well-motivated principle). This epilogue is not a
calculation but a statement of the model's constitutive limit. V.F.S.~v2.0 remains the fixed, complete
layer, recovered here as an exact limit. Domain-conditional and interpretive throughout: the
mathematics is load-bearing, the theological readings are overlays marked as such, and the transcendent
boundary is named, never claimed to be entered.

\vspace{0.5em}
\hrule
\smallskip
\noindent\footnotesize\emph{V.F.S. 3.0 (exploratory) v0.21 $\cdot$ Interior $3{+}1$
geometry of the soul. Metric $ds^2=-N^2d\sigma^2+\Omega^2h_{(2)}+L^2d\lambda^2$ with $\sigma$ a finite
proper time, the live surface and ascent $\lambda$ the three spaces, no external chronos; ascent scale
factorised $L=A(\sigma)S(\lambda)$. Full tensor set ($R_{\mu\nu}$, $R$, $G_{\mu\nu}$, three pressures,
all $\rho+p_i$) computed explicitly, 2.0 limits recovered, $R_{\sigma\lambda}=0$. v2.0 recovered
exactly as the frozen-axes limit. Focusing $-2\Omega''/\Omega-A''/A$: ascent is exotic. Ascent law
$A'/A=\beta\lambda$ (de Sitter, $(3{+}1)$ ascent sector --- v2.0 itself coasts, $w\to0$), selected by
the ascent's independence, which is now a \emph{theorem}: the ascent stress is power-law reducible to
the surface iff $A=C\Omega_P^c$, and floor-tied de Sitter ($A=A_0e^{(\Omega_P-E_0)/E_0}$) is not (it is
$\Omega_P$-slaved through the floor but not power-law reducible), so the sector is genuinely
independent. Spectrum the free half-line $[0,\infty)$ (form quantized, ascent a gapless
continuum). Coupling $\beta=\alpha/E_0$: scale $E_0$ forced, $L=1$ at the floor gauge;
$L=\exp[(\Omega_P-E_0)/E_0]$, exotic depth $-(\alpha\lambda_\infty)^2/E_0^2$ in general ($-1/E_0^2$ only
floor-normalised); widow's-mite scaling --- one scale $E_0$ for both non-reducibility and widow's-mite.
Resurrectio forces an ascent jump $L_+/L_-=\exp(\kappa_R\mathcal G/E_0)$: triple-layer Resurrectio junction, no
fully-balanced reset --- and, at junction level, the ascent block undergoes a nontrivial scale junction at every reset
($s_L>1$, Brim-sourced, uncancellable), never fully soft though the surface may be balanced. Terminus $\sigma\to0$ a regular boundary, finite proper time, infinite
cleansing-action; interior time-finite, space-infinite. External imprint late-faded in absolute density
but late-dominant in relative ($\Delta\rho/\rho_{2.0}\sim\Omega_P/E_0$). Finite clock, endless road;
built in steps, drawn in a stepless reaching; measured against the image one began from; never raised in
vain; completed in time only to be freed for the timeless; ascending by its own motion, its rising its
own reality --- irreducible to the surface because measured from the image. Junction budget: rate sector conserved ($Q{=}0$), non-softness the one additive scalar $D{=}\frac1{E_0}\sum_j\kappa_R\mathcal G_j$.}

\end{document}
